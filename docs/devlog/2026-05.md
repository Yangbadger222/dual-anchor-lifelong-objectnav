# Devlog - 2026-05

## 2026-05-24 - Initialize Agent Workflow Documentation

File:
- `AGENTS.md`
- `README.md`
- `.gitignore`
- `docs/README.md`
- `docs/design/2026-05-24-agent-workflow.md`
- `docs/design/README.md`
- `docs/devlog/README.md`
- `docs/experiments/README.md`
- `docs/handoff/README.md`
- `docs/decisions/README.md`
- `docs/templates/design_doc.md`
- `docs/templates/devlog_entry.md`
- `docs/templates/handoff.md`
- `docs/templates/experiment_report.md`
- `docs/templates/decision_record.md`

Change:
- Added the initial repository-level agent workflow and documentation templates.
- Added the design doc for the workflow system itself.
- Defined required design, devlog, handoff, experiment, and decision-record practices.
- Added a basic `.gitignore` for local metadata, runtime outputs, datasets, model weights, and logs.

Reason:
- The project needs strong continuity rules before implementation begins, because the target system is a long-running research project with offline development, later ROS 2 integration, and eventual paper-oriented experiments.

Verification:
- Inspected repository file structure with `find . -maxdepth 3 -type f -not -path './.git/*' -print | sort`.
- Checked Git state with `git status --short --branch`.
- Searched for unresolved placeholder markers across `AGENTS.md`, `README.md`, `docs`, and `.gitignore`.

Effect:
- Future agents and contributors have a clear operating contract before adding code.
- Non-trivial work must now leave design intent, verification evidence, and handoff context.

Follow-up:
- Create the first system architecture design doc before implementing ObjectNav modules.

## 2026-05-24 - Define Hardware-Independent ObjectNav Architecture

File:
- `docs/design/2026-05-24-system-architecture.md`
- `docs/design/2026-05-24-system-architecture.zh.html`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-system-architecture.md`

Change:
- Added the first formal system architecture design for Dual-Anchor Lifelong Semantic ObjectNav.
- Defined a three-layer development path: hardware-independent core, replay/simulation harness, and ROS 2 robot adapters.
- Captured core interfaces, future ROS 2 interfaces, data flow, failure modes, verification phases, and research relevance.
- Added a Chinese HTML reading version for easier review.
- Added a handoff note for the next implementation session.
- Refined Phase 1 into an indoor-first water-dispenser closed loop with outdoor-compatible anchor interfaces.
- Added fixed front-facing camera assumptions, verification viewpoint planning, stale/missing states, and relocation evidence behavior.
- Added Phase 1A fixture, forward-sector reveal, frontier viewpoint, discrete navigation, Pydantic model, SQLite/JSON memory, trial logging, metrics, and two-check missing confirmation decisions.
- Added a related-work positioning section covering OK-Robot, GOAT, 3D-Mem, DynaMem, OpenIN, SCOPE, R2F, SysNav, TrajRAG, and NavFoM.
- Added explicit research pain points: stale memory, missing-vs-localization ambiguity, model cost in high-frequency loops, verification viewpoints, dual-anchor uncertainty, and auditable trial logs.
- Added a staged model-integration policy that keeps Phase 1A deterministic and delays detector/VLM integration until offline replay evaluation.
- Updated the Chinese HTML reading version with the same related-work, pain-point, and model-integration guidance.

Reason:
- The project should develop and test the ObjectNav system away from the physical vehicle first, then integrate with the real FAST-LIO2 + PGO + Nav2 stack through stable adapters.
- This boundary improves reproducibility, baseline design, and paper defensibility.
- Recent ObjectNav work already uses semantic memory and foundation models, so the design must make the project's differentiation explicit before implementation starts.

Verification:
- Checked required documentation files exist with `test -f`.
- Inspected the design document headings with `rg -n "^## |^### " docs/design/2026-05-24-system-architecture.md`.
- Parsed the HTML reading page with Python `HTMLParser` and verified all internal navigation anchors resolve.
- Checked that the design Markdown and HTML reading page do not contain external links.
- Re-checked unresolved placeholders across the new docs and devlog with `rg`.
- Attempted a Playwright render check through the Node REPL, but `playwright` is not installed in the local Node environment; HTML verification used parser and anchor checks instead.
- Re-ran Markdown phase-heading checks after Phase 1A refinement.
- Re-parsed the HTML reading page with Python `HTMLParser` after adding the Phase 1A section and verified all internal navigation anchors resolve.
- Re-ran placeholder scans after Phase 1A refinement.
- Re-parsed the HTML reading page with Python `HTMLParser` after Phase 1A simulation/backend refinement and verified all internal navigation anchors resolve.
- Re-ran placeholder scans after Phase 1A simulation/backend refinement.
- Searched the design and handoff docs for the new fixture, reveal model, navigation backend, Pydantic, SQLite/JSON, trial logging, and yaw-scan terms.
- Checked arXiv page titles for the related-work links used in the design update.
- Re-parsed the HTML reading page after adding related-work/model-integration sections and verified all internal navigation anchors resolve.
- Re-ran placeholder scans after the related-work/model-integration update.

Effect:
- Future implementation can start from a documented architecture instead of coupling directly to robot-specific launch files, sensors, maps, or routes.
- The next concrete step is to write an implementation plan for the Phase 1A indoor water-dispenser closed loop.
- The paper positioning is clearer: the project should emphasize dynamic/lifelong memory repair, dual-anchor uncertainty, and hardware-independent reproducibility rather than claiming novelty from semantic memory alone.

Follow-up:
- Create the Phase 1A implementation plan for the deterministic indoor water-dispenser ObjectNav closed loop.
- Keep detector/VLM integration out of the main loop until deterministic Phase 1A and replay-based Phase 5 interfaces are verified.

## 2026-05-24 - Implement Phase 1A ObjectNav Core Slice

File:
- `.gitignore`
- `pyproject.toml`
- `README.md`
- `docs/design/2026-05-24-system-architecture.md`
- `docs/superpowers/plans/2026-05-24-phase1a-objectnav-core.md`
- `src/objectnav_core/objectnav_core/__init__.py`
- `src/objectnav_core/objectnav_core/models/__init__.py`
- `src/objectnav_core/objectnav_core/mapping/grid.py`
- `src/objectnav_core/objectnav_core/mapping/fixtures.py`
- `src/objectnav_core/objectnav_core/mapping/frontiers.py`
- `src/objectnav_core/objectnav_core/planning/viewpoints.py`
- `src/objectnav_core/objectnav_core/planning/scoring.py`
- `src/objectnav_core/objectnav_core/simulation/observations.py`
- `src/objectnav_core/objectnav_core/simulation/navigation.py`
- `src/objectnav_core/objectnav_core/simulation/trials.py`
- `src/objectnav_core/package.xml`
- `src/objectnav_core/setup.cfg`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/resource/objectnav_core`
- `src/objectnav_core/objectnav_core/memory/sqlite_store.py`
- `src/objectnav_core/objectnav_core/evaluation/logger.py`
- `src/objectnav_core/objectnav_core/cli/__init__.py`
- `src/objectnav_core/objectnav_core/cli/run_phase1a.py`
- `src/objectnav_core/tests/test_models.py`
- `src/objectnav_core/tests/test_mapping.py`
- `src/objectnav_core/tests/test_simulation.py`
- `src/objectnav_core/tests/test_trials.py`
- `src/objectnav_core/tests/test_cli_runner.py`
- `src/objectnav_core/tests/test_ros_packaging.py`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Installed `pytest` in the current Python environment after the user requested it.
- Added `.pytest_cache/` to `.gitignore`.
- Added `pytest` as a dev optional dependency in `pyproject.toml`.
- Added a Phase 1A implementation plan and switched it from `unittest` to `pytest`.
- Added a ROS-free `objectnav_core` Python package.
- Added ROS 2 `ament_python` package metadata for `objectnav_core` so the core can later be built by `colcon` on a ROS 2 machine.
- Added Pydantic models for anchors, poses, scene config, object observations, memory records, trial events, and trial metrics.
- Added the `straight_corridor_one_water_dispenser_unknown` grid fixture, forward-sector reveal, frontier extraction, frontier viewpoint planning, and wall-adjacent verification viewpoint planning.
- Added fake config-truth object observations and a deterministic discrete-step navigation client.
- Added SQLite memory storage with object records, observations, relations, trial events, indexes, and JSON export.
- Added a Phase 1A trial runner covering `discover_and_verify`, `reuse_same_start`, `reuse_different_start`, and `missing_and_relocation`.
- Added a CLI artifact runner exposed as `python -m objectnav_core.cli.run_phase1a --output ...`.
- Added future ROS 2 console script metadata for `objectnav_phase1a`.
- Added artifact outputs: `memory.sqlite`, `summary.json`, `memory_snapshot.json`, and `events.jsonl`.
- Added README quick-start instructions for running the Phase 1A tests.

Reason:
- The project needed a first executable vertical slice that proves the architecture independently from the real robot, detectors, VLMs, RTK, and Nav2.
- The implementation keeps the Phase 1A scope deterministic so later model and robot integration can be measured against a stable baseline.

Verification:
- Ran `python3 -m pytest src/objectnav_core/tests/test_models.py -v` and observed the expected initial import failure before implementing models.
- Ran `python3 -m pytest src/objectnav_core/tests/test_models.py -v`; 3 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests/test_mapping.py -v` and observed the expected missing-module failure before implementing mapping.
- Ran `python3 -m pytest src/objectnav_core/tests/test_mapping.py -v`; 4 tests passed after fixing frontier viewpoint standoff.
- Ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v` and observed the expected missing-module failure before implementing simulation.
- Ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v`; 3 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v` and observed the expected missing-module failure before implementing memory/trials.
- Ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v`; 2 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 12 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran a ROS-coupling scan for `rclpy`, Nav2, TF, ROS messages, and costmap topic strings under `src/objectnav_core`; no matches were found.
- Ran a placeholder scan; matches were limited to historical devlog text, the plan's verification step, and the local SQL variable name `placeholders`.
- Ran `python3 -m pytest src/objectnav_core/tests/test_ros_packaging.py -v`; 1 test passed, confirming `package.xml`, `setup.py`, and the ament resource marker exist without adding `rclpy` runtime imports.
- Ran `python3 -m pytest src/objectnav_core/tests/test_cli_runner.py -v` and observed the expected missing-module failure before implementing the CLI.
- Ran `python3 -m pytest src/objectnav_core/tests/test_cli_runner.py -v`; 1 test passed.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; command wrote the expected Phase 1A artifacts.
- Ran `python3 -m pytest src/objectnav_core/tests -v` after adding the CLI runner; 14 tests passed.

Effect:
- Phase 1A now has a runnable, tested, hardware-independent core slice.
- The core is packaged for future ROS 2 `ament_python` integration while remaining testable on this non-ROS computer.
- The first object memory reuse and missing/relocation state transitions are executable, though still deterministic and simplified.
- The deterministic trials can now be run outside pytest and saved as experiment artifacts.
- Future detector, replay, ROS 2, and Nav2 work can build against existing interfaces instead of replacing the core.

Follow-up:
- Add richer metrics persistence in `trial_metrics`.
- Add A* grid navigation before Nav2 adapter work.
- Add replay-image perception adapters only after the deterministic slice remains stable.

## 2026-05-24 - Add Phase 1A Visual Experiment Report

File:
- `docs/design/2026-05-24-phase1a-visual-report.md`
- `runs/phase1a/latest/report.html`
- `docs/devlog/2026-05.md`

Change:
- Added a design note for the Phase 1A static visual experiment report.
- Added a Chinese HTML report that visualizes the latest Phase 1A trial metrics, workflow, memory states, relocation relation, artifact files, and current limitations.
- The report is stored beside the experiment artifacts under `runs/phase1a/latest/`.

Reason:
- The project needs a readable experiment artifact that explains both the current results and the workflow without requiring ROS, a web server, or a frontend framework.

Verification:
- Parsed `runs/phase1a/latest/report.html` with Python `HTMLParser`.
- Verified all internal navigation anchors resolve.
- Compared the report against `summary.json` and `memory_snapshot.json` for the four trial ids and final memory states.
- Confirmed the report file exists under `runs/phase1a/latest/report.html`.

Effect:
- The current Phase 1A result can be reviewed visually from a single local HTML file.
- The workflow is clearer for future baseline, A*, and ROS adapter discussions.

Follow-up:
- Add a report generator CLI once the artifact schema stabilizes further.
- Add event timelines when `events.jsonl` payloads contain richer timestamps and transition details.

## 2026-05-24 - Persist Phase 1A Trial Metrics

File:
- `src/objectnav_core/objectnav_core/memory/sqlite_store.py`
- `src/objectnav_core/objectnav_core/simulation/trials.py`
- `src/objectnav_core/tests/test_trials.py`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Added SQLite APIs to record, read, and list `TrialMetrics` rows in the existing `trial_metrics` table.
- Updated the Phase 1A trial runner so every trial return path persists metrics before returning a `TrialResult`.
- Added regression tests for direct metrics persistence and for the four-run Phase 1A runner writing all trial metrics.
- Updated the Phase 1A handoff so `trial_metrics` persistence is no longer listed as unfinished.

Reason:
- The Phase 1A handoff identified persisted `TrialMetrics` rows as the next required build step.
- Reports, baseline comparisons, and later experiment records need metrics available in SQLite, not only in transient model instances or `summary.json`.

Verification:
- First ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v` and observed the expected failures for missing `record_trial_metrics` and `list_trial_metrics`.
- Re-ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v`; 3 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 15 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; command wrote the Phase 1A artifacts.
- Queried `runs/phase1a/latest/memory.sqlite` directly and confirmed 4 `trial_metrics` rows for the four deterministic trials.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.

Effect:
- The deterministic Phase 1A artifact database now contains queryable metrics for all four trial ids.
- Future report generators and baseline comparisons can consume metrics from SQLite rather than relying only on `summary.json`.

Follow-up:
- Add an A* navigation client while keeping the current `NavigationClient` behavior compatible.
- Add baseline policy switches for nearest frontier and information gain after A* is stable.

## 2026-05-24 - Add A* Grid Navigation Backend

File:
- `src/objectnav_core/objectnav_core/simulation/navigation.py`
- `src/objectnav_core/tests/test_simulation.py`
- `docs/design/2026-05-24-astar-grid-navigation.md`
- `docs/superpowers/plans/2026-05-24-astar-grid-navigation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Added a ROS-free `AStarGridNavigationClient` beside the existing straight-line discrete client.
- The A* client plans over 4-connected known free cells in `OccupancyGrid`; unknown and occupied cells are treated as blocked.
- The client keeps the existing navigation-client shape: `send_goal`, `cancel_goal`, `tick`, `status`, `path_length_m`, and `result_reason`.
- Added tests for obstacle detours, unknown-gap failure, and cancellation behavior.
- Added a design document and implementation plan for the A* backend.

Reason:
- The project needs a deterministic offline navigation backend for reachability and path-cost checks before ROS 2 Nav2 integration.
- A* supports reproducible baseline and ablation work while keeping the real robot execution boundary reserved for a future Nav2 adapter.

Verification:
- First ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v` and observed the expected import failure for missing `AStarGridNavigationClient`.
- Re-ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v`; 6 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 18 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; command wrote the Phase 1A artifacts.
- Queried `runs/phase1a/latest/memory.sqlite` directly and confirmed 4 successful `trial_metrics` rows remained present.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.

Effect:
- Offline navigation can now distinguish reachable, blocked, and unknown-space goals without ROS 2.
- The default Phase 1A trials still use the existing discrete client, so this change does not alter current artifact metrics.
- Future baseline policies can use A* path length instead of straight-line distance when scoring frontier and memory candidates.

Follow-up:
- Wire A* path-cost queries into baseline policy switches for nearest frontier and information gain.
- Add a ROS 2 Nav2 adapter design only after the offline backend and baseline policy behavior are stable.

## 2026-05-24 - Add Baseline Frontier Policy Switches

File:
- `src/objectnav_core/objectnav_core/planning/frontier_policies.py`
- `src/objectnav_core/objectnav_core/simulation/trials.py`
- `src/objectnav_core/tests/test_simulation.py`
- `src/objectnav_core/tests/test_trials.py`
- `docs/design/2026-05-24-baseline-frontier-policies.md`
- `docs/superpowers/plans/2026-05-24-baseline-frontier-policies.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Added `FrontierPolicyName`, `FrontierPolicyCandidate`, A* path-cost estimation, and `select_frontier_candidate`.
- Added explicit `first_frontier`, `nearest_frontier`, and `information_gain` frontier policy behavior.
- Updated `Phase1ATrialRunner` to accept an optional `frontier_policy` while preserving the default `first_frontier` behavior.
- Added frontier-selection event payload fields for policy, candidate type, information gain, A* path cost, revisit penalty, and score.
- Added tests for nearest-frontier selection, information-gain selection, and the Phase 1A runner information-gain switch.
- Added a baseline policy design document and implementation plan.

Reason:
- The architecture and handoff both identified repeatable nearest-frontier and information-gain baselines as the next offline step.
- Baseline policies need explicit score terms before comparing memory-aware or semantic ObjectNav behavior.

Verification:
- First ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v` and observed the expected missing-module failure for `objectnav_core.planning.frontier_policies`.
- Re-ran `python3 -m pytest src/objectnav_core/tests/test_simulation.py -v`; 8 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v` and observed the expected constructor failure before adding `frontier_policy`.
- Re-ran `python3 -m pytest src/objectnav_core/tests/test_trials.py -v`; 4 tests passed.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 21 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; command wrote the Phase 1A artifacts.
- Queried `runs/phase1a/latest/memory.sqlite` directly and confirmed 4 successful `trial_metrics` rows remained present.
- Confirmed the default `discover_and_verify` candidate types remain `frontier` and `object_verification`.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.

Effect:
- Future offline runs can explicitly select `nearest_frontier` or `information_gain` without changing ROS or Nav2 code.
- Baseline candidate scores now include enough structured terms for later reports and ablations.
- The default Phase 1A CLI artifact contract remains stable.

Follow-up:
- Add experiment report generation from `summary.json`, `events.jsonl`, and persisted `trial_metrics`, including policy score terms.
- Draft the ROS 2 Nav2 adapter design after offline report generation is stable.

## 2026-05-24 - Add Phase 1A Report Generator

File:
- `README.md`
- `runs/phase1a/latest/report.html`
- `runs/phase1a/latest/summary.json`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/generate_phase1a_report.py`
- `src/objectnav_core/objectnav_core/cli/run_phase1a.py`
- `src/objectnav_core/objectnav_core/evaluation/report.py`
- `src/objectnav_core/tests/test_cli_runner.py`
- `src/objectnav_core/tests/test_ros_packaging.py`
- `docs/design/2026-05-24-phase1a-report-generator.md`
- `docs/superpowers/plans/2026-05-24-phase1a-report-generator.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Added `generate_phase1a_report(artifact_dir)` to render `report.html` from `summary.json`, `memory_snapshot.json`, `events.jsonl`, and SQLite `trial_metrics`.
- Added `python -m objectnav_core.cli.generate_phase1a_report --input ...`.
- Added future ROS 2 console-script metadata for `objectnav_phase1a_report`.
- Updated `run_phase1a()` so `report.html` is generated automatically and appears in `summary["artifact_files"]`.
- Updated the CLI artifact test to verify report contents and internal HTML anchors.
- Updated README with the new report artifact and standalone report-regeneration command.
- Added a report-generator design document and implementation plan.

Reason:
- The existing visual report was a one-off HTML artifact and could drift from current metrics, event payloads, and memory state.
- Baseline score terms and persisted trial metrics now need a repeatable human-readable report path.

Verification:
- First ran `python3 -m pytest src/objectnav_core/tests/test_cli_runner.py -v` and observed the expected failure because `report.html` was not in the artifact manifest.
- Re-ran `python3 -m pytest src/objectnav_core/tests/test_cli_runner.py -v`; 1 test passed.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 21 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; command wrote all Phase 1A artifacts including `report.html`.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.generate_phase1a_report --input runs/phase1a/latest`; command rewrote `report.html`.
- Parsed generated HTML with `HTMLParser` and verified all internal navigation anchors resolve.
- Queried `runs/phase1a/latest/memory.sqlite` directly and confirmed 4 successful `trial_metrics` rows remained present.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.

Effect:
- Phase 1A now has a reproducible HTML report artifact generated from the same evidence files used by tests and future ablations.
- The report includes frontier policy score terms, object memory states, relocation evidence, artifact manifest, and persisted metrics summary.
- The artifact contract now includes `report.html`.

Follow-up:
- Draft the ROS 2 Nav2 adapter design after reviewing the generated report workflow.
- Consider exposing frontier policy selection in `run_phase1a` once multiple baseline runs are needed.

## 2026-05-24 - Draft ROS 2 Nav2 Adapter Design

File:
- `docs/design/2026-05-24-ros2-nav2-adapter.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`

Change:
- Added a design document for a future `objectnav_ros` package that will connect the ROS-free ObjectNav core to ROS 2, TF, OccupancyGrid costmaps, RViz markers, and Nav2 `NavigateToPose`.
- Defined the adapter/core boundary, default topics and actions, adapter parameters, data flow, failure modes, and verification plan.
- Kept the implementation scope design-only so `objectnav_core` remains free of ROS dependencies on this non-ROS machine.

Reason:
- Offline metrics persistence, A* path-cost checks, baseline frontier policies, and report generation are now stable enough to define the ROS integration boundary.
- The project needs an explicit Nav2 adapter design before creating ROS package code, so robot-specific assumptions do not leak into the research core.

Verification:
- Ran `test -f docs/design/2026-05-24-ros2-nav2-adapter.md`.
- Ran `rg -n "^## |^### " docs/design/2026-05-24-ros2-nav2-adapter.md` and reviewed the expected design headings.
- Ran `python3 -m pytest src/objectnav_core/tests -v`; 21 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran a core-only ROS-coupling scan for `rclpy`, Nav2, TF, ROS messages, and costmap topic strings under `src/objectnav_core/objectnav_core`; no matches were found.
- Ran a placeholder scan; matches were limited to templates, historical verification notes, XML package metadata, and the local SQL `placeholders` variable.

Effect:
- The next contributor can create an `objectnav_ros` adapter skeleton against a documented package layout, topic/action surface, and verification checklist.
- The ROS integration path is now explicit without changing core code or requiring a local ROS installation.

Follow-up:
- Create an implementation plan for the minimal `objectnav_ros` skeleton in a ROS 2 environment or container.
- Decide the first ObjectNav goal and object-observation message shapes before live robot integration.
- Add adapter replay tests before any Nav2 robot trial.

## 2026-05-24 - Add ROS 2 Adapter Skeleton

File:
- `.gitignore`
- `README.md`
- `docs/design/2026-05-24-ros2-nav2-adapter.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-phase1a-objectnav-core.md`
- `docs/handoff/2026-05-24-ros2-nav2-adapter-skeleton.md`
- `runs/phase1a/latest/report.html`
- `runs/phase1a/latest/summary.json`
- `src/objectnav_core/objectnav_core/models/__init__.py`
- `src/objectnav_ros/package.xml`
- `src/objectnav_ros/setup.py`
- `src/objectnav_ros/setup.cfg`
- `src/objectnav_ros/resource/objectnav_ros`
- `src/objectnav_ros/objectnav_ros/__init__.py`
- `src/objectnav_ros/objectnav_ros/adapters/__init__.py`
- `src/objectnav_ros/objectnav_ros/adapters/costmap_adapter.py`
- `src/objectnav_ros/objectnav_ros/adapters/nav2_navigation_client.py`
- `src/objectnav_ros/objectnav_ros/adapters/object_observation_adapter.py`
- `src/objectnav_ros/objectnav_ros/adapters/pose_adapter.py`
- `src/objectnav_ros/objectnav_ros/nodes/__init__.py`
- `src/objectnav_ros/objectnav_ros/nodes/objectnav_node.py`
- `src/objectnav_ros/config/indoor_nav2_adapter.yaml`
- `src/objectnav_ros/launch/objectnav_adapter.launch.py`
- `src/objectnav_ros/tests/test_costmap_adapter.py`
- `src/objectnav_ros/tests/test_import_boundaries.py`
- `src/objectnav_ros/tests/test_nav2_status_mapping.py`
- `src/objectnav_ros/tests/test_object_observation_adapter.py`
- `src/objectnav_ros/tests/test_pose_adapter.py`

Change:
- Restored the missing `objectnav_core.models` package with Pydantic models for anchors, poses, scene config, object observations, memory records, events, metrics, and the default corridor scene.
- Fixed `.gitignore` so the source package `src/objectnav_core/objectnav_core/models/` is no longer hidden by the root model-weight ignore rule.
- Added root-level ignores for ROS 2 `log/`, `build/`, and `install/` outputs.
- Updated the ROS 2/Nav2 adapter design to reflect the current ROS 2 Humble environment and the first buildable skeleton scope.
- Added a new `objectnav_ros` `ament_python` package.
- Added adapter modules for OccupancyGrid conversion, PoseStamped/TransformStamped conversion, Nav2 goal/status conversion, and JSON `std_msgs/String` object-observation conversion with stale timestamp rejection.
- Added a minimal `rclpy` node shell with configurable goal, observation, status, selected-goal, costmap, Nav2 action, frame, anchor, memory, and stale-observation parameters.
- Added launch/config files and adapter unit tests.
- Regenerated the latest Phase 1A summary and report after restoring the core scene model.

Reason:
- The environment now has ROS 2 Humble, so the next handoff item was to create a buildable adapter skeleton instead of leaving ROS integration design-only.
- The core model package had been hidden by an over-broad `.gitignore` pattern, which made the repository unable to reproduce its own tests from tracked files.
- The adapter must translate ROS types at the boundary while keeping ObjectNav state and memory logic ROS-free.

Verification:
- Observed the initial plain `python3 -m pytest` failure caused by a system pytest/anyio plugin mismatch: `ModuleNotFoundError: No module named '_pytest.scope'`.
- Used `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` for local pytest runs.
- Observed that disabling plugins requires explicit `PYTHONPATH=src/objectnav_core` because this system pytest does not honor the `pythonpath` config option.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 21 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_ros/tests -q`; 12 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_core/tests src/objectnav_ros/tests -q`; 33 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core src/objectnav_ros/objectnav_ros`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core:${PYTHONPATH} python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; all four deterministic Phase 1A runs succeeded and report artifacts were regenerated.
- Ran a core-only ROS-coupling scan for `rclpy`, ROS messages, TF, Nav2, and visualization imports under `src/objectnav_core/objectnav_core`; no matches were found.
- Ran `colcon build --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; both packages built successfully.
- Ran an installed-space import smoke test after `source install/setup.zsh`; core scene creation and `NavigateToPose` goal conversion succeeded.
- Ran `python3 -m py_compile src/objectnav_ros/launch/objectnav_adapter.launch.py`; command exited successfully.
- Parsed `src/objectnav_ros/config/indoor_nav2_adapter.yaml` with PyYAML and confirmed `map_frame`.
- Ran `source install/setup.zsh && ros2 pkg executables objectnav_ros && ros2 pkg prefix objectnav_ros`; ROS 2 found the `objectnav_adapter` executable and package prefix.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; 21 core tests and 12 adapter tests passed.
- Ran `colcon test-result --verbose`; summary reported 33 tests, 0 errors, 0 failures, 0 skipped.

Effect:
- The repository now has a reproducible ROS 2 package boundary: `objectnav_core` remains ROS-free, while `objectnav_ros` owns ROS message and Nav2 translation.
- Future Nav2 integration can add action-client and replay-driven node behavior without changing the core models or trial runner.
- The source model package is now tracked normally, preventing a repeat of the hidden-core-module failure.

Follow-up:
- The action-client wrapper follow-up is handled by the later "Add Mock-Testable Nav2 Action Client" entry.
- Add synthetic rosbag or message replay tests for costmap, TF pose, object observations, status output, and selected-goal publication.
- Decide whether to keep JSON strings for early object observations or introduce a custom message/action package before live robot trials.

## 2026-05-24 - Add Mock-Testable Nav2 Action Client

File:
- `README.md`
- `docs/design/2026-05-24-ros2-nav2-adapter.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-ros2-nav2-adapter-skeleton.md`
- `src/objectnav_ros/objectnav_ros/adapters/nav2_navigation_client.py`
- `src/objectnav_ros/objectnav_ros/nodes/objectnav_node.py`
- `src/objectnav_ros/tests/test_nav2_status_mapping.py`

Change:
- Added `Nav2NavigationClient`, a thin asynchronous wrapper around Nav2 `NavigateToPose`.
- The wrapper waits for the action server, sends goals, records feedback messages, handles goal acceptance/rejection, maps action results to core `NavigationStatus`, preserves Nav2 error details on failures, and supports cancel requests.
- Updated `objectnav_adapter` node initialization so it owns a configured Nav2 client without automatically sending navigation goals.
- Added fake-action-client tests for unavailable server, successful result, rejected goal, aborted result detail, and cancel request behavior.
- Updated README, design, and handoff notes to reflect that a mock-testable action client exists, while live Nav2 and robot tests remain future work.

Reason:
- The previous adapter only had message conversion and status mapping. The next integration step needed an explicit Nav2 action boundary that can be tested without a live robot or Nav2 server.
- Keeping the wrapper in `objectnav_ros` preserves the ROS-free core boundary.

Verification:
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_ros/tests/test_nav2_status_mapping.py -q`; 7 tests passed.
- Ran `python3 -m compileall -q src/objectnav_ros/objectnav_ros src/objectnav_ros/tests`; command exited successfully.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_ros/tests -q`; 17 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_core/tests src/objectnav_ros/tests -q`; 38 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core src/objectnav_ros/objectnav_ros`; command exited successfully.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.
- Ran `colcon build --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; both packages built successfully.
- Ran an installed-space `Nav2NavigationClient` smoke test with a fake unavailable server; it returned `FAILED` and `action_server_unavailable`.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; 21 core tests and 17 adapter tests passed.
- Ran `colcon test-result --verbose`; summary reported 38 tests, 0 errors, 0 failures, 0 skipped.
- Ran `PYTHONPATH=src/objectnav_core:${PYTHONPATH} python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; 4 runs, all successful.

Effect:
- The ROS adapter now has a test-covered action-client boundary ready for mocked action-server and replay tests.
- The node can be launched with a configured Nav2 client, but it still does not drive the robot or execute the ObjectNav state machine.

Follow-up:
- Add a mocked action-server or ROS launch-test harness for end-to-end action behavior.
- The synthetic replay follow-up is handled by the later "Add ROS 2 Synthetic Replay And RViz Markers" entry.
- Wire the node to core candidate selection only after replay tests pass.

## 2026-05-25 - Add ROS 2 Synthetic Replay And RViz Markers

File:
- `README.md`
- `docs/design/2026-05-24-ros2-nav2-adapter.md`
- `docs/design/2026-05-25-ros2-synthetic-replay-rviz.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-24-ros2-nav2-adapter-skeleton.md`
- `docs/handoff/2026-05-25-ros2-synthetic-replay-rviz.md`
- `src/objectnav_ros/package.xml`
- `src/objectnav_ros/setup.py`
- `src/objectnav_ros/config/indoor_nav2_adapter.yaml`
- `src/objectnav_ros/launch/objectnav_adapter.launch.py`
- `src/objectnav_ros/launch/synthetic_replay.launch.py`
- `src/objectnav_ros/launch/synthetic_replay_rviz.launch.py`
- `src/objectnav_ros/rviz/synthetic_replay.rviz`
- `src/objectnav_ros/objectnav_ros/adapters/costmap_adapter.py`
- `src/objectnav_ros/objectnav_ros/adapters/visualization.py`
- `src/objectnav_ros/objectnav_ros/nodes/objectnav_node.py`
- `src/objectnav_ros/objectnav_ros/nodes/synthetic_replay_node.py`
- `src/objectnav_ros/tests/test_costmap_adapter.py`
- `src/objectnav_ros/tests/test_objectnav_node_replay.py`
- `src/objectnav_ros/tests/test_rviz_config.py`
- `src/objectnav_ros/tests/test_synthetic_replay.py`
- `src/objectnav_ros/tests/test_visualization.py`
- `runs/phase1a/latest/report.html`
- `runs/phase1a/latest/summary.json`

Change:
- Added a design document for the synthetic replay and RViz-marker slice.
- Added `core_grid_to_occupancy_grid_msg` for ROS replay fixtures.
- Added `objectnav_ros.adapters.visualization` for frontier, selected-goal, and memory marker construction.
- Added `objectnav_synthetic_replay`, which publishes the Phase 1A corridor costmap, a target-class goal, a synthetic object observation, and `map -> base_link` TF.
- Added `synthetic_replay.launch.py` to launch both `objectnav_adapter` and `objectnav_synthetic_replay`.
- Added `synthetic_replay.rviz` and `synthetic_replay_rviz.launch.py` for one-command RViz inspection.
- Updated `objectnav_adapter` so replayed costmaps produce frontier markers and a first reachable selected frontier goal, and accepted object observations produce memory markers.
- Added tests for costmap roundtrip, synthetic replay message builders, marker helpers, adapter-node replay callbacks, and RViz config contents.
- Updated README and handoff notes with the replay launch command and remaining RViz verification gap.

Reason:
- Before Gazebo or real-robot testing, the ROS adapter needs an inspectable replay path that catches topic, frame, marker, and costmap-threshold errors.
- RViz markers make adapter failures visible without adding perception, Nav2 control, Gazebo physics, or vehicle-specific launch files.

Verification:
- Ran focused synthetic replay/visualization tests; 11 tests passed.
- Ran adapter replay callback test; 1 test passed.
- Ran RViz config tests; 2 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_ros/tests -q`; 28 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_core/tests src/objectnav_ros/tests -q`; 49 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core src/objectnav_ros/objectnav_ros`; command exited successfully.
- Ran `python3 -m py_compile src/objectnav_ros/launch/objectnav_adapter.launch.py src/objectnav_ros/launch/synthetic_replay.launch.py src/objectnav_ros/launch/synthetic_replay_rviz.launch.py`; command exited successfully.
- Ran a core-only ROS-coupling scan; no matches were found under `src/objectnav_core/objectnav_core`.
- Ran `PYTHONPATH=src/objectnav_core:${PYTHONPATH} python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; 4 runs, all successful.
- Ran a synthetic message smoke test; generated a 2880-cell costmap, JSON object observation, and `map->base_link` TF.
- Ran `colcon build --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; both packages built successfully.
- Ran installed-space smoke checks; ROS 2 found `objectnav_adapter` and `objectnav_synthetic_replay`, and synthetic builders worked from installed modules.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; 21 core tests and 28 adapter tests passed.
- Ran `colcon test-result --verbose`; summary reported 49 tests, 0 errors, 0 failures, 0 skipped.
- Ran `timeout 6s bash -lc 'source install/setup.bash && ros2 launch objectnav_ros synthetic_replay.launch.py'`; both ROS nodes started, and exit code 124 was expected because `timeout` stopped the launch.
- Ran a topic smoke test against the launched replay nodes; status, selected goal, frontier marker topic, and memory marker topic were available.
- Ran `timeout 8s bash -lc 'source install/setup.bash && ros2 launch objectnav_ros synthetic_replay_rviz.launch.py'`; adapter, replay, and RViz started, RViz reported OpenGL support and costmap creation, and exit code 124 was expected because `timeout` stopped the launch.

Effect:
- The ROS adapter now has a runnable synthetic replay path and RViz-visible outputs for costmap-derived frontiers, selected goals, and memory observations.
- The system can be inspected without Gazebo, Nav2 live execution, detector models, or real robot hardware.

Follow-up:
- Open RViz for manual visual inspection and save a screenshot or verification record.
- Save a verification record or screenshot for the RViz inspection.
- Add a mocked Nav2 action server or launch-test harness after visual replay is confirmed.

## 2026-05-25 - Tune Synthetic Replay Readability And Shutdown

File:
- `README.md`
- `docs/design/2026-05-25-ros2-synthetic-replay-rviz.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-25-ros2-synthetic-replay-rviz.md`
- `src/objectnav_ros/rviz/synthetic_replay.rviz`
- `src/objectnav_ros/objectnav_ros/adapters/visualization.py`
- `src/objectnav_ros/objectnav_ros/nodes/objectnav_node.py`
- `src/objectnav_ros/objectnav_ros/nodes/synthetic_replay_node.py`
- `src/objectnav_ros/tests/test_rviz_config.py`
- `src/objectnav_ros/tests/test_synthetic_replay.py`
- `src/objectnav_ros/tests/test_visualization.py`

Change:
- Changed the synthetic replay from a static corridor snapshot to a step-based replay with progressive costmap reveal and moving `map -> base_link` transform.
- Changed synthetic observations so they are published only after the replay robot has detector range/FOV/line-of-sight visibility.
- Added `/objectnav/debug_markers` with a small RViz text legend showing replay step and base pose.
- Switched the replay costmap publisher and RViz config to reliable transient-local costmap QoS for cleaner late-joiner behavior.
- Added focused tests for progressive reveal, visibility-gated observation, replay-pose TF, debug marker contents, and RViz config topics.
- Cleaned `objectnav_adapter` and `objectnav_synthetic_replay` SIGINT handling so launch shutdown does not produce rclpy double-shutdown tracebacks.

Reason:
- The first RViz screenshot was technically correct but visually ambiguous: it looked like a static map with a goal and did not explain which parts were replay scaffolding.
- Before moving to Gazebo or robot tests, the replay should make the adapter boundary easier to inspect and should exit cleanly during repeated launch-smoke runs.

Verification:
- Ran focused replay/RViz tests; 13 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_core/tests src/objectnav_ros/tests -q`; 53 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core src/objectnav_ros/objectnav_ros`; command exited successfully.
- Ran `python3 -m py_compile src/objectnav_ros/launch/objectnav_adapter.launch.py src/objectnav_ros/launch/synthetic_replay.launch.py src/objectnav_ros/launch/synthetic_replay_rviz.launch.py`; command exited successfully.
- Ran a core-only ROS-coupling scan for ROS imports under `src/objectnav_core/objectnav_core`; no matches were found.
- Ran `PYTHONPATH=src/objectnav_core:${PYTHONPATH} python3 -m objectnav_core.cli.run_phase1a --output runs/phase1a/latest`; 4 runs, all successful.
- Ran `colcon build --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; both packages built.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; 21 core tests and 32 adapter tests passed.
- Ran `colcon test-result --verbose`; summary reported 53 tests, 0 errors, 0 failures, 0 skipped.
- Ran isolated-domain topic smoke with `ROS_DOMAIN_ID=45`; `/objectnav/status`, `/objectnav/selected_goal`, `/objectnav/debug_markers`, and `/global_costmap/costmap` were available, and the costmap publisher reported `RELIABLE` + `TRANSIENT_LOCAL`.
- Ran isolated-domain RViz launch smoke with `ROS_DOMAIN_ID=44`; `objectnav_adapter`, `objectnav_synthetic_replay`, and `rviz2` started, RViz created the 120 x 24 map, and all processes finished cleanly after SIGINT.

Effect:
- RViz replay now communicates what it is showing: moving robot context, progressive exploration, a selected frontier goal, delayed object observation, and a legend.
- Repeated launch-smoke verification is cleaner because expected SIGINT shutdown no longer looks like a node crash.

Follow-up:
- Close any old RViz/replay launch before re-launching; older processes may still show the previous volatile costmap publisher and static replay behavior.
- Save a repo-local RViz screenshot or verification record.
- Add a mocked Nav2 action server or launch-test harness before trying Gazebo or real robot motion.

## 2026-05-25 - Decide TurtleBot3 Gazebo Assumed-Target Path

File:
- `docs/design/2026-05-25-turtlebot3-gazebo-assumed-target.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-25-turtlebot3-gazebo-assumed-target.md`

Change:
- Reviewed the current architecture, ROS 2/Nav2 adapter design, synthetic replay design, and handoff notes before choosing the next integration direction.
- Added a design document for a TurtleBot3 Gazebo + Nav2 smoke slice that treats `water_dispenser` as an assumed configured map-frame target pose instead of requiring detection.
- Added a handoff documenting local TurtleBot3/Nav2/Gazebo package availability, required environment variables, map files, commands already run, risks, and next implementation steps.

Reason:
- The current synthetic replay proves adapter topic and RViz visibility, but it does not exercise a live Nav2 action server.
- A TurtleBot3 open-source simulation is a better next Gazebo step than a vehicle-specific stack because it is reproducible, hardware-independent, and already installed locally.
- Deferring perception is deliberate: using an assumed target pose isolates the Nav2 execution boundary before detection noise can hide integration bugs.

Verification:
- Ran `git status --short --branch`.
- Read `README.md`, `docs/design/2026-05-24-ros2-nav2-adapter.md`, `docs/design/2026-05-25-ros2-synthetic-replay-rviz.md`, `docs/handoff/2026-05-25-ros2-synthetic-replay-rviz.md`, `docs/design/2026-05-24-system-architecture.md`, and related handoff/design docs.
- Ran `source install/setup.zsh && ros2 pkg list | rg -i "turtlebot|nav2|gazebo|slam_toolbox|map_server|amcl"`; TurtleBot3, Nav2, Gazebo ROS, map server, AMCL, and slam_toolbox packages were present.
- Ran `which gazebo`, `which gz`, `which ign`, and `which ros2`; all expected commands were present.
- Listed installed TurtleBot3 Gazebo and Navigation2 launch, world, map, parameter, and RViz files under `/opt/ros/humble/share`.
- Ran `source install/setup.zsh && TURTLEBOT3_MODEL=waffle ros2 launch turtlebot3_navigation2 navigation2.launch.py --show-args`; launch arguments printed successfully.
- Observed that the same Navigation2 launch fails with `KeyError: 'TURTLEBOT3_MODEL'` when that environment variable is unset.
- Inspected `/opt/ros/humble/share/turtlebot3_navigation2/map/map.yaml`; the map has nonzero origin `[-10.0, -10.0, 0.0]`.
- Inspected `src/objectnav_ros/objectnav_ros/adapters/costmap_adapter.py` and confirmed current core grid conversion does not preserve `OccupancyGrid.info.origin`.

Effect:
- The next Gazebo step is now explicit: use TurtleBot3 + open-source map + configured assumed `water_dispenser` pose to test live Nav2 action execution.
- The design avoids prematurely mixing perception, map-origin frontier selection, and full ObjectNav state-machine integration into the first simulator run.

Follow-up:
- Implement `assumed_target_nav2_smoke` and a TurtleBot3 launch wrapper.
- Choose and verify one reachable target pose in the TurtleBot3 map.
- Add origin-aware map/costmap conversion before using full frontier exploration on TurtleBot3 maps.

## 2026-05-25 - Implement TurtleBot3 Assumed-Target Nav2 Smoke

File:
- `README.md`
- `docs/design/2026-05-25-turtlebot3-gazebo-assumed-target.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-25-turtlebot3-gazebo-assumed-target.md`
- `src/objectnav_ros/package.xml`
- `src/objectnav_ros/setup.py`
- `src/objectnav_ros/config/turtlebot3_assumed_targets.yaml`
- `src/objectnav_ros/launch/turtlebot3_assumed_target_nav2.launch.py`
- `src/objectnav_ros/objectnav_ros/nodes/assumed_target_nav2_smoke.py`
- `src/objectnav_ros/tests/test_assumed_target_nav2_smoke.py`

Change:
- Added `assumed_target_nav2_smoke`, a thin ROS node that maps a configured semantic class (`water_dispenser`) to a map-frame `Pose2D`, publishes `/objectnav/selected_goal`, waits for `/objectnav/goal`, and sends the pose through `Nav2NavigationClient`.
- Added `turtlebot3_assumed_targets.yaml` with a first assumed `water_dispenser` target at `(x=1.5, y=0.0, yaw=0.0)` in `map`.
- Added `turtlebot3_assumed_target_nav2.launch.py`, which sets `TURTLEBOT3_MODEL=waffle` and can start TurtleBot3 Gazebo, TurtleBot3 Navigation2, and the smoke node.
- Kept `send_goal_on_start=false` by default so AMCL can be initialized before triggering the semantic goal.
- Added tests for target validation, trigger matching, status JSON, node trigger behavior, config presence, launch references, and entry-point installation.
- Updated README, design, and handoff instructions for the TurtleBot3 assumed-target smoke.

Reason:
- The synthetic replay path verifies adapter topics and RViz outputs, but it does not exercise a live Nav2 action server.
- TurtleBot3 simulation is the lowest-risk open-source bridge to test the Nav2 action boundary before Gazebo worlds with custom semantics or real robot trials.
- Treating `water_dispenser` as a configured pose deliberately avoids mixing perception, projection, and navigation execution in the first simulator smoke.

Verification:
- Ran focused assumed-target tests; 5 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core:src/objectnav_ros:${PYTHONPATH} python3 -m pytest src/objectnav_core/tests src/objectnav_ros/tests -q`; 58 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core src/objectnav_ros/objectnav_ros`; command exited successfully.
- Ran `python3 -m py_compile src/objectnav_ros/launch/objectnav_adapter.launch.py src/objectnav_ros/launch/synthetic_replay.launch.py src/objectnav_ros/launch/synthetic_replay_rviz.launch.py src/objectnav_ros/launch/turtlebot3_assumed_target_nav2.launch.py`; command exited successfully.
- Ran a core-only ROS import scan; no matches were found under `src/objectnav_core/objectnav_core`.
- Ran `colcon build --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; both packages built.
- Ran installed-space checks; ROS 2 found `assumed_target_nav2_smoke`, and `turtlebot3_assumed_target_nav2.launch.py --show-args` exposed the expected launch arguments.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+`; 21 core tests and 37 adapter tests passed.
- Ran `colcon test-result --verbose`; summary reported 58 tests, 0 errors, 0 failures, 0 skipped.
- Ran isolated node-only launch smoke with `start_gazebo:=false start_nav2:=false use_sim_time:=false`; `/objectnav/status` and `/objectnav/selected_goal` were published from installed space.
- Ran isolated trigger smoke without a Nav2 server; publishing `water_dispenser` to `/objectnav/goal` produced an `action_server_unavailable` status as expected.

Effect:
- The project now has a concrete, reproducible entry point for the first live Nav2 action-server test using open-source TurtleBot3 assets.
- The first Gazebo smoke can proceed without requiring object detection or a custom simulated water-dispenser model.

Follow-up:
- Run the full TurtleBot3 Gazebo/Nav2 launch, set the initial pose in RViz, trigger `/objectnav/goal`, and record the Nav2 result.
- If the assumed pose is blocked or unreachable, adjust only `turtlebot3_assumed_targets.yaml`.
- Add origin-aware occupancy-grid conversion before using core frontier exploration on TurtleBot3 maps.

## 2026-05-25 - Complete TurtleBot3 Gazebo Nav2 Smoke

File:
- `README.md`
- `docs/design/2026-05-25-turtlebot3-gazebo-assumed-target.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-25-turtlebot3-gazebo-assumed-target.md`
- `src/objectnav_ros/launch/turtlebot3_assumed_target_nav2.launch.py`
- `src/objectnav_ros/tests/test_assumed_target_nav2_smoke.py`

Change:
- Renamed the wrapper launch argument from the generic `params_file` path to `nav2_params_file` so it cannot leak into Gazebo's nested `gzserver.launch.py`.
- Added launch-time Gazebo Classic model lookup hardening: the wrapper defaults `GAZEBO_MODEL_DATABASE_URI` to empty and prepends local TurtleBot3/Gazebo model paths before including TurtleBot3 Gazebo.
- Added a focused test assertion that the launch contains the Gazebo model-database mitigation and the `nav2_params_file` argument.
- Updated the design and handoff records with the successful full TurtleBot3 Gazebo/Nav2 run.

Reason:
- The first full run failed before TurtleBot spawn because Gazebo inherited the Nav2 params file through a colliding `params_file` launch argument.
- The next full run still failed because Gazebo Classic stalled on the online model database, so `/spawn_entity` never appeared.
- The smoke needs to be reproducible offline before it is useful as the bridge from synthetic replay to real robot work.

Verification:
- Ran a direct Gazebo factory probe and reproduced the missing `/spawn_entity` condition while Gazebo tried `http://models.gazebosim.org`.
- Ran a Gazebo probe with `GAZEBO_MODEL_DATABASE_URI=""`; `/spawn_entity`, `/delete_entity`, and `/get_model_list` appeared after 2 seconds.
- Ran `python3 -m py_compile src/objectnav_ros/launch/turtlebot3_assumed_target_nav2.launch.py`; command exited successfully.
- Ran `bash -lc 'source /opt/ros/humble/setup.bash; source install/setup.bash; PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -q src/objectnav_ros/tests/test_assumed_target_nav2_smoke.py'`; 5 tests passed.
- Ran `bash -lc 'source /opt/ros/humble/setup.bash; colcon build --packages-select objectnav_ros --symlink-install'`; package built successfully.
- Ran the full TurtleBot3 Gazebo/Nav2 smoke in `ROS_DOMAIN_ID=78`; `/spawn_entity` was ready after 2 seconds, `/odom` and `/navigate_to_pose` were available, the initial pose was set to `x=-2.0`, `y=-0.5`, `yaw=0.0`, and triggering `water_dispenser` produced `/objectnav/status` with `navigation_status=SUCCEEDED` and `result_reason=nav2_succeeded`.
- Ran `bash -lc 'source /opt/ros/humble/setup.bash; PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 colcon test --packages-select objectnav_core objectnav_ros --event-handlers console_direct+'`; 21 core tests and 37 ROS tests passed.
- Ran `colcon test-result --verbose`; summary reported 58 tests, 0 errors, 0 failures, 0 skipped.

Effect:
- The project now has a verified open-source simulator bridge from assumed semantic target to live Nav2 action execution.
- The `water_dispenser` assumed target at `(x=1.5, y=0.0, yaw=0.0)` is verified reachable in the TurtleBot3 map when initialized at `(-2.0, -0.5, 0.0)`.
- Gazebo Classic startup no longer depends on the online model database for this wrapper launch.

Follow-up:
- Add origin-aware `OccupancyGrid` handling before feeding TurtleBot3 maps into full core frontier exploration.
- Consider turning the successful smoke procedure into a documented manual script or launch test.
- Keep detector/perception integration deferred until the Nav2 and map-frame contracts remain stable.

## 2026-05-26 - Redesign Algorithm Around Usability-Centered Object Memory

File:
- `docs/design/2026-05-26-usability-centered-lifelong-object-memory.md`
- `docs/design/2026-05-26-usability-centered-lifelong-object-memory.zh.html`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`

Change:
- Added a new design document that reframes the algorithmic contribution from dual-anchor alignment to usability-centered lifelong object memory.
- Added a Chinese HTML reading version explaining each algorithm step and the main derivations.
- Defined a memory belief split into `P_existence`, `P_location_valid`, and `P_usable`.
- Defined delayed birth, delayed promotion, evidence classification, event-driven usability update, finite-budget trust/verify/search/retire decision-making, path-cost cache invalidation, and opportunistic verification.
- Explicitly demoted dual-anchor RTK/LIO alignment to infrastructure rather than the main paper contribution.
- Added a handoff note for the next implementation/design iteration.
- Updated the design with the real-vehicle boundary: `dual-anchor-lifelong-objectnav` owns algorithm/replay, while `XJTLU-autonomous-vehicle` owns RTK, RGB-D, Nav2, FAST-LIO2/PGO, rosbag collection, and live-robot closure.
- Added concrete XJTLU launch modes, rosbag topics, future RGB-D/detector interfaces, and an intermediate `TraceEvent` contract.
- Updated the HTML reading version with an XJTLU interface section and MathJax display-math support for rendered formulas.

Reason:
- The dual-anchor architecture was strong as a systems direction but too close to engineering integration as an algorithmic paper contribution.
- Real RGB-D and SLAM conditions make perfect FREE-space negative evidence rare, so the new design focuses on memory usability and retirement rather than proving object disappearance.
- The method needs a clear path for single-researcher progress: deterministic simulation, real trace collection, trace-driven replay, then small live robot closure.

Verification:
- Created the Markdown and HTML files.
- Re-checked the updated Markdown for the new XJTLU interface section.
- Re-checked by text search that the updated docs contain the XJTLU interface section, relevant ROS topics, `TraceEvent`, and MathJax configuration.
- Scanned the new design, HTML, and handoff files for unresolved template placeholders; no matches were found.
- An earlier lightweight HTML parser/anchor check passed before the later XJTLU/MathJax update; it was not repeated after the user requested not to check HTML.
- Did not run browser rendering because the user asked not to check HTML rendering.
- Did not run runtime tests because this task only changed documentation.

Effect:
- The next implementation target is now the smallest testable usability-memory slice: add `P_usable`, retired/archive state, non-confirmation evidence, and trust/verify/search/retire decision metrics to the ROS-free core.
- Future detector, RGB-D, RTK, and ROS integration can be evaluated against the usability-memory algorithm instead of becoming the paper's core novelty.

Follow-up:
- Implement a deterministic Phase 1A extension for usability belief and memory retirement.
- Add a trace schema for RGB-D/detector/pose/costmap evidence collection.
- Use real logs to measure FREE/OCCLUDED/UNKNOWN/NON_CONFIRMATION ratios before any full live-robot ObjectNav closure.

## 2026-05-26 - Add Usability Memory Stress Harness

File:
- `README.md`
- `docs/devlog/2026-05.md`
- `docs/experiments/2026-05-26-usability-memory-stress-harness.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_usability_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/usability_stress.py`
- `src/objectnav_core/objectnav_core/memory/usability.py`
- `src/objectnav_core/tests/test_ros_packaging.py`
- `src/objectnav_core/tests/test_usability_memory.py`
- `src/objectnav_core/tests/test_usability_stress.py`

Change:
- Added a ROS-free usability-memory model with `P_existence`, `P_location_valid`, `P_usable`, evidence events, and a bounded trust/verify/search/retire decision policy.
- Added deterministic tests for ghost-memory retirement, false-deletion protection, positive recovery, quarantined negative evidence, and expected-cost decisions.
- Added a stress runner that writes `summary.json`, `decision_boundary.csv`, and `stress_report.html` under an output directory.
- Registered the runner as the `objectnav_usability_stress` console script and documented the command in the README.
- Added an experiment report for the first fixed-seed synthetic run.

Reason:
- RTK and RGB-D integration are not ready yet, but the core mathematical assumptions can already be attacked offline.
- The project needs a small executable harness before adding real detector/depth/SLAM traces, otherwise the algorithm discussion stays too abstract to falsify.
- The first slice deliberately avoids ROS, Nav2, RTK, GroundingDINO, and real robot dependencies.

Verification:
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_usability_memory.py src/objectnav_core/tests/test_usability_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 10 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 30 tests passed.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_usability_stress --output runs/usability_stress/latest --seed 13 --monte-carlo-runs 200`; command exited successfully and wrote `summary.json`, `decision_boundary.csv`, and `stress_report.html`.
- The fixed-seed stress run produced decision counts: trust `58`, verify `10`, search `102`, retire `30`.
- The stress run confirmed the three intended synthetic scenario outcomes: ghost retired, false-deletion guard not retired after positive recovery, and quarantined OOD negative evidence unchanged.

Effect:
- There is now a concrete pre-robot algorithm test target for the new usability-centered route.
- Future detector, depth, pose, and path-cost interfaces can be validated by converting their outputs into evidence events and replaying them through this harness.
- The generated stress artifacts live under ignored `runs/usability_stress/latest/` and can be regenerated from the recorded command.

Follow-up:
- Add adversarial stress cases for nearby same-class objects and wrong data association.
- Add stale cost-cache and path blockage cases before relying on `D_verify` in real-robot decisions.
- Convert future XJTLU RGB-D/RTK/Nav2 logs into trace events and compare real evidence ratios against these synthetic assumptions.

## 2026-05-26 - Fix Budget-Clipped Trust Cost Pathology

File:
- `docs/design/2026-05-26-usability-centered-lifelong-object-memory.md`
- `docs/design/2026-05-26-usability-centered-lifelong-object-memory.zh.html`
- `docs/devlog/2026-05.md`
- `docs/experiments/2026-05-26-usability-memory-stress-harness.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`
- `src/objectnav_core/objectnav_core/memory/usability.py`
- `src/objectnav_core/tests/test_usability_memory.py`

Change:
- Added a regression test for a fixed-seed sample where `P_v=0.0324`, `D_verify=2.3922`, and `C_search=57.5906`.
- Removed per-branch `min(B_remaining, ...)` clipping from the trust/search/verify expected-cost calculation.
- Updated the design docs to state that `B_remaining` is a feasibility constraint and diagnostic, not an expected-cost cap.
- Updated the experiment report with the corrected fixed-seed decision counts.

Reason:
- Budget clipping made likely failure look artificially cheap. In the failing sample, trust won because both trust and verify were clipped near `B_remaining`, leaving verify to look worse only because it paid the extra verification cost.
- This creates the bad behavior the design was meant to avoid: when budget is low, the robot gambles on a nearly invalid memory instead of acknowledging the failure branch cost.

Verification:
- Ran the new regression test before the fix; it failed with `DecisionType.TRUST`.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_usability_memory.py -q`; 9 tests passed after the fix.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 31 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_usability_stress --output runs/usability_stress/latest --seed 13 --monte-carlo-runs 200`; command exited successfully.
- The corrected fixed-seed stress run produced decision counts: trust `6`, verify `19`, search `136`, retire `39`.
- The problematic sample now has `cost_trust=76.22`, `cost_verify=58.52`, `cost_search=57.59`, and no longer chooses trust.

Effect:
- Low remaining budget no longer converts a high-probability failed trust action into an artificially cheap gamble.
- The current pure expected-cost policy may still prefer search over verify when `P_v` is extremely low. Choosing opportunistic verification before search requires an explicit memory-maintenance or information-value term, not hidden budget clipping.

Follow-up:
- Add a separate design/test for opportunistic verification value if the system should verify cheap old memories before global search.
- Add budget feasibility reporting so actions whose raw expected cost exceeds `B_remaining` are visible without distorting their costs.

## 2026-05-26 - Add 2D Grid Trace Generator Experiment

File:
- `README.md`
- `docs/design/2026-05-26-2d-grid-trace-generator.md`
- `docs/devlog/2026-05.md`
- `docs/experiments/2026-05-26-2d-grid-trace-generator.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_grid_trace_experiment.py`
- `src/objectnav_core/objectnav_core/evaluation/grid_trace_experiment.py`
- `src/objectnav_core/tests/test_grid_trace_experiment.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Added a design document for the lightweight 2D grid trace generator layer.
- Added `generate_grid_trace`, which emits fixed-seed sequential trace events with robot pose, target pose, evidence type, evidence strength, path-blocked state, association-candidate count, and navigation costs.
- Added `run_grid_trace_experiment`, which replays trace events through `UsabilityUpdater` and `UsabilityDecisionPolicy`.
- Added a CLI entry point, `objectnav_grid_trace_experiment`.
- Added tests for deterministic generation, required trace schema, artifact writing, and packaging.
- Documented the command in the README and recorded the first fixed-seed experiment report.

Reason:
- The isolated usability stress harness is useful but does not produce sequential traces.
- A fast 2D generator lets the project attack logic failures before Habitat noisy-depth replay and XJTLU real-bag replay are ready.
- The trace schema gives future Habitat/rosbag converters a concrete target.

Verification:
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_grid_trace_experiment.py -q`; 2 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_ros_packaging.py -q`; 1 test passed.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_grid_trace_experiment --output runs/grid_trace/latest --seed 17 --episodes 12 --steps-per-episode 8`; command exited successfully and wrote `summary.json`, `events.csv`, and `trace_report.html`.
- The fixed-seed run produced 96 events and decision counts: trust `20`, verify `61`, search `2`, retire `13`.
- The fixed-seed run covered evidence types: positive `20`, non-confirmation `16`, unknown `25`, occluded `14`, access-blocked `11`, free `8`, scene-changed `2`.
- The OOD scenario produced 16 quarantined events and left belief unchanged.
- The removed/moved and blocked-access scenarios retired with final `P_usable` below `0.1`.

Effect:
- The project now has a reproducible pre-Habitat trace layer that exercises sequential belief and decision behavior.
- Future Habitat noisy-depth and XJTLU bag replay should target the same intermediate event columns instead of integrating directly into the belief update.

Follow-up:
- Add stale path-cost and inflated-obstacle blockage scenarios.
- Add true multi-object association replay beyond the current `association_candidates` field.
- Replace hand-set event probabilities with Habitat and real-bag statistics as soon as those traces exist.

## 2026-05-26 - Add Inflation and Stale Path-Cost Monte Carlo

File:
- `README.md`
- `docs/design/2026-05-26-2d-grid-trace-generator.md`
- `docs/devlog/2026-05.md`
- `docs/experiments/2026-05-26-2d-grid-trace-generator.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`
- `src/objectnav_core/objectnav_core/evaluation/grid_trace_experiment.py`
- `src/objectnav_core/tests/test_grid_trace_experiment.py`

Change:
- Added `inflated_corridor_block` and `stale_path_cost` scenarios to the 2D trace generator.
- Added path-cost fields for obstacle/body intersection, inflation-layer intersection, stale-cost state, cached path costs, fresh path costs, and costmap revision.
- Added stale-vs-refreshed decision logging with `decision_stale`, `decision_refreshed`, `decision_flipped_after_refresh`, and `stale_cache_error`.
- Changed the grid trace experiment runner to stream event rows directly to CSV while accumulating summaries, so 100000 episodes can run without storing all rows in memory.
- Added summary metrics for decision rates, inflation-blocked events, stale-cost events, decision flips after refresh, stale cache error rate, and mean cached-to-fresh cost ratio.
- Updated the README and experiment report to use the 100000-episode Monte Carlo command.

Reason:
- The first 12-episode trace had a verify-heavy bias because verification cost was usually much lower than search cost.
- Inflation-layer blockage and stale path-cost cache are the most direct way to stress the budget/path-cost decision logic before Habitat or real robot data exists.

Verification:
- Ran the updated grid trace tests before the implementation; they failed because the new scenarios and path-cost metrics were missing.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_grid_trace_experiment.py -q`; 2 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 33 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_grid_trace_experiment --output runs/grid_trace/latest --seed 17 --episodes 100000 --steps-per-episode 8`; command exited successfully.
- The 100000-episode run produced 800000 events with decision counts: trust `139377`, verify `365502`, search `147338`, retire `147783`.
- The run produced path-cost metrics: inflation-blocked events `175000`, stale-cost events `100000`, decision flips after refresh `149923`, stale cache error rate `0.875`, and mean cached-to-fresh cost ratio `15.7098`.
- Checked artifact size: `events.csv` has 800001 lines including the header and is about 242 MB.

Effect:
- Verify remains the largest decision class, but the decision distribution is no longer collapsed around verify. Search and retire each reached about 18%.
- The 2D layer now directly tests the stale path-cost failure mode that can make a real robot over-verify or trust a bad route.
- The generated large Monte Carlo artifacts stay under ignored `runs/grid_trace/latest/`.

Follow-up:
- Add true multi-object association replay beyond `association_candidates`.
- Sweep inflation radius, cache refresh thresholds, and opportunistic verification value.
- Replace synthetic path-cost transitions with Habitat and real XJTLU costmap-derived transitions when available.

## 2026-05-26 - Add Multi-Object Association Replay

File:
- `README.md`
- `docs/design/2026-05-26-2d-grid-trace-generator.md`
- `docs/devlog/2026-05.md`
- `docs/experiments/2026-05-26-2d-grid-trace-generator.md`
- `docs/handoff/2026-05-26-usability-centered-lifelong-object-memory.md`
- `src/objectnav_core/objectnav_core/evaluation/grid_trace_experiment.py`
- `src/objectnav_core/tests/test_grid_trace_experiment.py`

Change:
- Added a `multi_object_association` scenario to the 2D trace generator.
- Added event fields for `true_memory_id`, `nearest_memory_id`, `jpda_memory_id`, `association_margin`, `association_entropy`, and `false_positive`.
- Added replay fields for `nearest_wrong_association`, `jpda_rejected_ambiguous`, and `ghost_positive_write_prevented`.
- Added association summary metrics: association events, nearest-neighbor wrong associations, nearest-neighbor wrong-association rate, JPDA-lite rejected ambiguous events, prevented ghost positive writes, and mean association margin.
- Updated the HTML report, design doc, experiment report, and handoff notes.

Reason:
- A scalar `association_candidates` count cannot test the failure mode where a nearest-neighbor association assigns a jittered or false-positive observation to the wrong memory.
- Before adding Habitat or real RGB-D logs, the 2D Monte Carlo should explicitly test whether ambiguity is rejected instead of being written into memory.

Verification:
- Ran the updated grid trace tests before implementation; they failed because `multi_object_association` and `association_metrics` were missing.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_grid_trace_experiment.py -q`; 2 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 33 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_grid_trace_experiment --output runs/grid_trace/latest --seed 17 --episodes 100000 --steps-per-episode 8`; command exited successfully.
- The 100000-episode run produced 800000 events with decision counts: trust `200798`, verify `336952`, search `130948`, retire `131302`.
- Association metrics: association events `177776`, nearest-neighbor wrong associations `22222`, JPDA-lite rejected ambiguous events `44444`, ghost positive writes prevented `22222`, nearest-neighbor wrong-association rate `0.125`, mean association margin `0.6336`.
- Path-cost metrics remained active: inflation-blocked events `155554`, stale-cost events `88888`, decision flips after refresh `133269`, stale cache error rate `0.875`, mean cached-to-fresh cost ratio `15.7189`.
- Checked artifact size: `events.csv` has 800001 lines including the header and is about 288 MB.

Effect:
- The 2D trace layer now tests wrong-association and false-positive memory-write failure modes, not just path-cost and evidence-type transitions.
- The experiment can report how many ghost writes nearest-neighbor association would have created and how many were rejected by the JPDA-lite gate.

Follow-up:
- Replace the hand-coded JPDA-lite margin gate with trace-calibrated association likelihoods.
- Add a true multi-hypothesis memory buffer that keeps unresolved ambiguous observations for later frames instead of only rejecting them.
- Use Habitat and XJTLU logs to estimate association-margin distributions.

## 2026-05-26 - Add Localization-Only Bag Audit

File:
- `README.md`
- `docs/design/2026-05-26-localization-only-bag-audit.md`
- `docs/experiments/2026-05-26-localization-only-bag-audit.md`
- `docs/handoff/2026-05-26-localization-only-bag-audit.md`
- `docs/superpowers/plans/2026-05-26-localization-only-bag-audit.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_localization_bag_audit.py`
- `src/objectnav_core/objectnav_core/evaluation/localization_bag_audit.py`
- `src/objectnav_core/tests/test_localization_bag_audit.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Added a ROS-runtime-free localization bag audit module.
- Added minimal CDR decoders for `NavSatFix`, `Odometry`, `Float64MultiArray`, and `String`.
- Added SQLite bag discovery, topic/message reads, FAST-LIO continuity metrics, GPS validity/jump metrics, nearest-time GPS-LIO pairing, 2D similarity alignment residuals, and anchor-health classification.
- Added artifact writers for `summary.json`, `session_metrics.csv`, `topic_counts.csv`, `fix_samples.csv`, `lio_samples.csv`, and `audit_report.html`.
- Added `objectnav_core.cli.run_localization_bag_audit` and the `objectnav_localization_bag_audit` console script entry.
- Added tests using a synthetic SQLite rosbag fixture.
- Added a 5 m motion-spread floor so short overfit alignments are classified as `weak_alignment` rather than `usable`.

Reason:
- Existing XJTLU bags do not contain the RGB-D evidence needed for ObjectNav memory validation, but they are valuable for replay plumbing and anchor-health diagnosis before RTK/depth hardware is ready.
- The project needs to prove that bad or weak G60 GPS can be diagnosed and rejected before it contaminates long-term object-memory anchors.

Verification:
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_localization_bag_audit.py -q` after adding the first tests and observed the expected missing-module failure.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_localization_bag_audit.py::test_localization_bag_audit_marks_small_motion_alignment_as_weak -q` after adding the spread-floor test and observed the expected `usable` vs `weak_alignment` failure before implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_localization_bag_audit.py -q`; 4 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_localization_bag_audit.py src/objectnav_core/tests/test_ros_packaging.py -q`; 4 tests passed.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_localization_bag_audit --output runs/localization_bag_audit/latest --bag /Users/badger/Desktop/my_local_data/logs/2026-03-25-17-46-15/bag --bag /Users/badger/Desktop/my_local_data/logs/2026-03-22-21-05-17/bag`; command exited successfully with health counts `gnss_rejected: 1`, `weak_alignment: 1`.
- Ran `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_localization_bag_audit --output runs/localization_bag_audit/all_sessions --data-root /Users/badger/Desktop/my_local_data/logs`; command exited successfully over 90 sessions with health counts `weak_alignment: 46`, `gnss_rejected: 17`, `lio_only: 17`, `usable: 9`, `insufficient_lio: 1`.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 37 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Checked generated artifacts under `runs/localization_bag_audit/latest` and `runs/localization_bag_audit/all_sessions`; expected JSON, CSV, and HTML files exist.

Effect:
- The algorithm repo can now consume existing XJTLU localization bags without ROS installed on the analysis machine.
- The first real-data evidence supports conservative anchor-health gating: G60 `/fix` can be valid and continuous while GPS-LIO alignment is still too weak or inconsistent for global memory anchoring.
- The generated run artifacts remain under ignored `runs/localization_bag_audit/`.

Follow-up:
- Add trajectory visualization for representative sessions.
- Re-run the same audit after RTK/depth-camera bags are recorded.
- Add a stable XJTLU trace sidecar export so future ObjectNav evidence extraction does not need to parse every bag directly.

## 2026-05-26 - Document Habitat-Sim Usability Replay Plan And Repository Layout

File:
- `.gitignore`
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-26-habitat-sim-usability-replay.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/simulation/README.md`
- `docs/simulation/2026-05-26-habitat-sim-usability-memory.zh.html`
- `docs/superpowers/plans/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`

Change:
- Added a detailed Chinese HTML operation guide for the Habitat-Sim usability-memory replay route.
- Added a formal design doc for the future Habitat-Sim replay layer.
- Added a simulation documentation directory and README.
- Added a repository file-management guide covering canonical directories, artifact policy, and Habitat dataset placement.
- Updated the root README and docs README to link the new simulation and file-management entry points.
- Expanded `.gitignore` for Habitat datasets, third-party clones, generated videos, model/data files, MCAP bags, nested `.DS_Store`, Python caches, and local temp/cache directories.
- Removed local `.DS_Store`, `__pycache__`, and `.pytest_cache` generated files.

Reason:
- The next algorithm validation layer should be Habitat-Sim, but the project needs a clean operating protocol and file policy before adding simulator dependencies or large assets.
- Habitat datasets, videos, and local clones are large/generated artifacts and must not enter git.
- The repository needed a clearer map now that Phase 1A, usability memory, grid traces, localization bag audits, ROS adapters, and simulation plans all coexist.

Verification:
- Confirmed the current project state with `git status --short --branch`.
- Listed docs and source files to inspect current layout before editing.
- Ran `git check-ignore -v` on representative `.DS_Store`, `runs/`, dataset, and `third_party` paths; the updated `.gitignore` matched them.
- Deleted local `.DS_Store`, `__pycache__`, and `.pytest_cache` generated files.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 37 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Confirmed the new Habitat HTML guide, repository file-management doc, design doc, handoff, and implementation plan files exist.
- Re-deleted caches after verification and confirmed `.DS_Store`, `__pycache__`, and `.pytest_cache` counts were zero.
- HTML rendering was not checked because the user explicitly said it was unnecessary.

Effect:
- Habitat-Sim work now has a clear documentation home and implementation route.
- Future large datasets should land under ignored `datasets/`, `scene_datasets/`, `object_datasets/`, or `habitat-data/`.
- Future local Habitat clones should land under ignored `third_party/` or `external/`.

Follow-up:
- Implement `habitat_trace_schema.py` and scalar evidence extraction tests before importing Habitat.
- Set up a separate Habitat conda environment on the execution machine.
- Run a 20-episode Habitat smoke before any larger benchmark-style sweep.

## 2026-05-27 - Record Habitat-Sim Environment Checkpoint

File:
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`

Change:
- Recorded the user's update that the development setup is now in a ROS 2 environment and the conda environment `habitat` is configured well enough to run simulation.
- Added a Habitat-Sim handoff checkpoint with the environment checks Codex could verify locally.
- Updated next Habitat steps to start from the existing ROS 2 Humble workspace and `habitat` conda environment rather than from environment creation.

Reason:
- The Habitat-Sim setup is a meaningful reproducibility milestone for the next simulation layer.
- Future work needs to distinguish the verified Habitat-Sim import boundary from the still-unverified Habitat-Lab/ObjectNav episode boundary.

Verification:
- Ran `git status --short --branch`; the branch was `main...origin/main` before edits.
- Verified ROS 2 command discovery: `which ros2` returned `/opt/ros/humble/bin/ros2`.
- Verified ROS 2 package lookup for `rclpy` and `nav2_bringup`.
- Verified that `source install/setup.zsh` exposes `objectnav_ros` and `objectnav_core` from this repository's `install/`.
- Verified conda environment discovery: `habitat` exists at `/home/badger/anaconda3/envs/habitat`.
- Verified `conda run -n habitat python --version` returns Python 3.9.23.
- Verified `conda run -n habitat python -c 'import habitat_sim ...'` imports Habitat-Sim 0.3.3.
- Observed that the current non-interactive shell has no active conda environment and bare `python` is not found.
- Observed that `conda run -n habitat python -c 'import habitat ...'` fails with `ModuleNotFoundError`, and `pip show` does not find `habitat-lab` or `habitat`.
- Did not run a Habitat scene load, ObjectNav episode, trace export, memory replay, or code test suite because this was a documentation checkpoint.

Effect:
- The next contributor can start from a verified ROS 2 + Habitat-Sim environment checkpoint instead of redoing environment discovery.
- The project record now explicitly warns that Habitat-Lab remains unverified even though Habitat-Sim is importable.

Follow-up:
- Decide whether the first smoke uses pure Habitat-Sim scene stepping or Habitat-Lab ObjectNav episodes.
- If Habitat-Lab episodes are needed, install or source Habitat-Lab in the `habitat` environment.
- Run and report a minimal Habitat-Sim scene-load smoke before claiming simulation results.

## 2026-05-27 - Add Habitat-Lab ObjectNav Smoke And Run HM3D Scene

File:
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-26-habitat-sim-usability-replay.md`
- `docs/experiments/2026-05-27-habitat-objectnav-smoke.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_smoke.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_smoke.py`
- `src/objectnav_core/tests/test_habitat_objectnav_smoke.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Installed Habitat-Lab v0.3.3 into the existing `habitat` conda environment and matched `pillow==10.4.0`.
- Installed `git-lfs` inside the `habitat` conda environment.
- Added `run_habitat_objectnav_smoke`, which creates a synthetic one-episode Habitat-Lab `ObjectNav-v1` dataset, runs deterministic actions, and exports `habitat_trace.csv`, `summary.json`, and `report.html`.
- Added optional scene-dataset config and navmesh-sampled synthetic start/target support for real Habitat scenes.
- Added tests that verify the smoke module does not import Habitat at normal import time and that trace/report helpers are stable.
- Documented the toy scene smoke and HM3D official-scene smoke.

Reason:
- The project needed to move from environment-only validation to an executable Habitat scene-load, ObjectNav task, and trace-export path.
- Keeping Habitat imports optional preserves the ROS-free core test boundary.
- HM3D example assets provide a real scene/navmesh/semantic-metadata preflight while HSSD/ProcTHOR ObjectNav episode downloads remain blocked and official HM3D ObjectNav benchmark execution still needs HM3D v0.1 scene assets.

Verification:
- Ran `conda run -n habitat python -m pip check`; no broken requirements found after installing `jinja2`, `typeguard`, and matching Pillow.
- Verified `habitat`, `habitat_sim`, and `gym` imports with versions 0.3.3, 0.3.3, and 0.23.0.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_smoke.py src/objectnav_core/tests/test_ros_packaging.py -q`; 5 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_smoke.py -q`; 4 tests passed after adding HM3D options.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `git diff --check`; command exited successfully.
- Ran the toy scene smoke on `/home/badger/Desktop/habitat-sim-main/data/test_assets/scenes/simple_room.glb`; it produced 6 trace rows, `episode_over: true`, mean depth `0.739173`, mean depth valid ratio `1.0`, and 2 collision steps.
- Downloaded `habitat_test_pointnav_dataset` successfully under ignored `datasets/habitat/`.
- Downloaded `hm3d_example_full` successfully under ignored `datasets/habitat/`; `datasets/habitat` is about 402 MB.
- Ran the HM3D official-scene smoke on `GLAQ4DNUx5U.basis.glb`; it produced 7 trace rows, `episode_over: true`, `navmesh_loaded: true`, `sampled_navigable_start: true`, semantic object count `908`, semantic category count `124`, mean depth `1.299647`, mean depth valid ratio `1.0`, and 0 collision steps.
- Confirmed ignored artifact files exist under `runs/habitat_usability/smoke` and `runs/habitat_usability/hm3d_official_scene_smoke`.
- Retrying `habitat_test_scenes` failed against Hugging Face with TLS/clone errors.
- DropBox ObjectNav episode links remained unavailable: HSSD objectnav timed out and ProcTHOR objectnav reset.

Effect:
- The repository now has a reproducible Habitat-Lab ObjectNav smoke path that runs on a real HM3D scene without making Habitat a core import dependency.
- The project has crossed from installation verification into real-scene simulation preflight, but it still lacks official ObjectNav benchmark episodes.
- Generated datasets and run artifacts remain under ignored `datasets/` and `runs/`.

Follow-up:
- Provide or retry official ObjectNav episode datasets under ignored `datasets/habitat/`.
- Add semantic sensor export for HM3D/HSSD scenes.
- Convert Habitat trace rows into `EvidenceEvent` rows consumed by `UsabilityUpdater`.
- Run official ObjectNav episodes with task measurements enabled once episode data is available.

## 2026-05-27 - Add Habitat Usability Replay And Run 30 Episodes

File:
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-26-habitat-sim-usability-replay.md`
- `docs/experiments/2026-05-27-habitat-objectnav-smoke.md`
- `docs/experiments/2026-05-27-habitat-usability-replay.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_smoke.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_usability_replay.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_smoke.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_usability_replay.py`
- `src/objectnav_core/tests/test_habitat_usability_replay.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Downloaded and extracted the official HM3D ObjectNav v1 episode archive under ignored `datasets/habitat/`.
- Added a local ignored `datasets/habitat/datasets/objectnav/hm3d/v1` symlink for the extracted episode split.
- Added `objectnav_habitat_usability_replay`, a multi-episode Habitat replay that converts Habitat trace rows into `EvidenceEvent` rows and runs `UsabilityUpdater` plus `UsabilityDecisionPolicy`.
- Added distance-checked near-target navmesh sampling for the synthetic smoke runner.
- Added replay tests and packaging assertions.
- Ran a 30-episode replay over the three downloaded HM3D example scenes.
- Added an experiment report and updated the Habitat design/handoff docs.

Reason:
- The user asked to download the missing assets where possible and run multiple complete simulations to see whether the algorithm is usable.
- Official ObjectNav episode data alone is insufficient without the official HM3D scene assets, so the repo needed a local multi-episode replay path that still exercises Habitat scene loading, RGB-D observations, navmesh sampling, collisions, evidence updates, and policy decisions.

Verification:
- Confirmed official HM3D ObjectNav `val_mini` contains 30 episodes over 2 scenes and `val` contains 2000 episodes over 20 scenes.
- Confirmed local official HM3D scene availability is 0 of 2 `val_mini` scenes and 0 of 20 `val` scenes.
- Attempted `hm3d_minival_v0.1` scene download; Habitat-Sim failed with `Username required`.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_smoke.py src/objectnav_core/tests/test_habitat_usability_replay.py src/objectnav_core/tests/test_ros_packaging.py -q`; 8 tests passed before the near-target guard.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_smoke.py src/objectnav_core/tests/test_habitat_usability_replay.py -q`; 7 tests passed after the near-target guard.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran `conda run -n habitat env PYTHONPATH=src/objectnav_core python -m objectnav_core.cli.run_habitat_usability_replay ... --episodes 30 --seed 101 --sensor-size 96`; command completed and wrote 210 replay rows.
- Final 30-episode replay metrics: evidence counts `positive=62`, `free=27`, `non_confirmation=44`, `access_blocked=47`, `unknown=30`; decision counts `verify=128`, `trust=26`, `search=27`, `retire=29`; scenario mean final `p_valid` was `0.814795` near-anchor, `0.303595` local-verify, and `0.113509` random-anchor.

Effect:
- The algorithm can now be exercised as an offline Habitat trace consumer rather than only as a pure unit-test or 2D-grid stress harness.
- The current evidence supports integration readiness and plausible policy behavior, but not official ObjectNav benchmark performance.
- Official HM3D ObjectNav episodes are ready locally once HM3D v0.1 scene assets are provided.

Follow-up:
- Provide Matterport/HM3D credentials or offline HM3D v0.1 val/minival scene assets.
- Run official HM3D ObjectNav `val_mini` with measurements enabled.
- Replace distance-only evidence proxies with semantic oracle visibility and then detector replay.

## 2026-05-27 - Add Habitat Semantic YOLO-Breaker Stress

File:
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-26-habitat-sim-usability-replay.md`
- `docs/experiments/2026-05-27-habitat-semantic-yolo-stress.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_semantic_yolo_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_semantic_yolo_stress.py`
- `src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Added a Habitat-Sim direct RGB-D-semantic stress runner.
- Added synthetic YOLO-breaker corruptions for semantic target masks: `miss`, `fly_point`, `edge_break`, and `mixed`, plus `clean` baseline.
- Added mask-quality metrics and evidence conversion for corrupted semantic masks.
- Added CLI entry point `objectnav_habitat_semantic_yolo_stress`.
- Added tests for optional Habitat imports, miss/fly-point breaker behavior, fragmented mask rejection, missed visible target handling, out-of-view target handling, structural-category filtering, and component stats.
- Ran a 5-episode smoke and a 30-episode semantic stress run on HM3D `GLAQ4DNUx5U`.
- Added an experiment report and updated Habitat design/handoff docs.

Reason:
- The previous Habitat replay used synthetic target distance as evidence. The user asked to use Habitat semantic sensor output and explicitly attack the usability-memory system with detector-like semantic-mask failures.
- Semantic masks provide a stronger intermediate layer before adding a real YOLO/GroundingDINO detector.

Verification:
- Verified Habitat-Sim direct semantic sensor observations on HM3D `GLAQ4DNUx5U`; the semantic mask is a `uint32` id image and the scene reports 908 semantic objects / 124 categories.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 7 tests passed after first implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py -q`; 8 tests passed after the out-of-view absence fix.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran a 5-episode semantic smoke; it produced all five breaker modes and showed miss collapse plus one mixed false-positive-positive row.
- Ran a 30-episode semantic stress after fixing out-of-view target absence to `UNKNOWN`; it completed with 210 rows.
- Final 30-episode metrics: evidence counts `positive=71`, `non_confirmation=13`, `unknown=126`; decision counts `verify=176`, `trust=34`; mean final `p_valid=0.819062`.
- Breaker mode means: clean `0.908939`, miss `0.610724`, fly-point `0.847893`, edge-break `0.866955`, mixed `0.860801`.
- Stress failures observed: mixed produced 9 false-positive-positive rows; miss produced 16 missed-visible-target rows.

Effect:
- The project now has a semantic-mask stress layer that is materially closer to detector failure than distance-proxy replay.
- The current memory policy handles many fragmented/edge-corrupt masks conservatively as `UNKNOWN`, but can still be inflated by single-frame false positives that survive quality gates.

Follow-up:
- Add temporal consistency and multi-view confirmation before accepting positive semantic/detector evidence.
- Add debug mask PNG export for representative corrupted frames.
- Replace corrupted oracle semantic masks with real detector replay.

## 2026-05-27 - Add Habitat ObjectNav Val-Mini Semantic Stress

File:
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-27-habitat-val-mini-semantic-stress.md`
- `docs/experiments/2026-05-27-habitat-objectnav-valmini-semantic-stress.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_valmini_semantic_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_valmini_semantic_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_valmini_semantic_stress.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Added an official HM3D ObjectNav `val_mini` semantic stress runner.
- Added `hm3d/val/...` to local `hm3d/habitat/...` scene path resolution without rewriting downloaded datasets.
- Added run-local Habitat scene dataset config generation for local HM3D semantic assets.
- Added target-category semantic-mask extraction for official ObjectNav categories.
- Added CLI entry point `objectnav_habitat_objectnav_valmini_semantic_stress`.
- Added tests for lazy Habitat imports, episode loading, path resolution, scene config generation, start-source selection, and category aliases.
- Ran a 1-episode probe and a 30-episode `val_mini` semantic stress run.

Reason:
- The user pointed out the local `scene_datasets/hm3d/habitat` and `scene_datasets/hm3d/semantic` assets, which include the two `val_mini` scenes.
- The previous semantic stress used one HM3D example scene and selected visible ids directly. The project needed a stronger bridge to official ObjectNav episode metadata while still avoiding unsupported success/SPL claims.

Verification:
- Confirmed `val_mini/content/TEEsavR23oF.json.gz` and `wcojb4TFT35.json.gz` contain 15 episodes each.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_valmini_semantic_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 8 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran a 1-episode probe with `--start-source goal_viewpoint`; it completed with target-visible rows and valid semantic metadata.
- Ran `conda run -n habitat env PYTHONPATH=src/objectnav_core python -m objectnav_core.cli.run_habitat_objectnav_valmini_semantic_stress ... --max-episodes 30 --start-source goal_viewpoint --seed 313 --sensor-size 96`; it completed with 210 trace rows.
- Final 30-episode metrics: evidence counts `positive=58`, `non_confirmation=9`, `unknown=143`; decision counts `verify=175`, `trust=35`; target-visible episodes `27`; mean final `p_valid=0.757149`.
- Breaker mode means: clean `0.970945`, miss `0.569045`, fly-point `0.780606`, edge-break `0.721548`, mixed `0.743600`.
- Stress failures observed: mixed produced 4 false-positive-positive rows; miss produced 11 missed-visible-target rows.

Effect:
- The project now has a reproducible official-episode semantic stress path that consumes HM3D ObjectNav `val_mini` metadata, local semantic scene assets, YOLO-breaker corruptions, and usability memory in one command.
- The result is stronger than the single-scene semantic stress because it uses official target categories and two official `val_mini` scenes.
- It is still not an official Habitat benchmark result because it uses goal viewpoints and scripted actions, not a navigation policy, and reports no success/SPL.

Follow-up:
- Run a separate `--start-source episode_start` pass to quantify target visibility from official starts under scripted actions.
- Add temporal or multi-view confirmation before accepting positive semantic/detector evidence.
- Add debug mask PNG export for low-precision mixed positives and missed visible targets.

## 2026-05-27 - Add Positive Confirmation And Episode-Start Val-Mini Pass

File:
- `README.md`
- `docs/README.md`
- `docs/repository-file-management.md`
- `docs/design/2026-05-27-habitat-val-mini-semantic-stress.md`
- `docs/experiments/2026-05-27-habitat-objectnav-valmini-semantic-stress.md`
- `docs/experiments/2026-05-27-habitat-valmini-episode-start-confirmation.md`
- `docs/handoff/2026-05-26-habitat-sim-usability-replay.md`
- `docs/devlog/2026-05.md`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_valmini_semantic_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_valmini_semantic_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_valmini_semantic_stress.py`

Change:
- Added positive confirmation to the official HM3D ObjectNav `val_mini` semantic stress runner.
- Separated raw/candidate evidence from final evidence that updates memory.
- Required positive candidates to repeat across time, viewpoint motion, and detector-mask IoU before becoming `POSITIVE`.
- Added CLI thresholds for confirmation frames, translation, rotation, and mask IoU.
- Added summary/report metrics for candidate positives, confirmed positives, suppressed positives, false-positive candidates, and final false-positive-positive rows.
- Ran a goal-viewpoint confirmation pass and an official `--start-source episode_start` pass.
- Added an experiment report comparing visibility and false-positive suppression.

Reason:
- The previous `val_mini` semantic stress found 4 low-precision positive rows that survived quality gates.
- The user asked to quantify official episode-start visibility and block single-frame false positives before trusting semantic memory updates.

Verification:
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_valmini_semantic_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 12 tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`; command exited successfully.
- Ran the 30-episode goal-viewpoint confirmation stress; it completed with 210 rows, 31 confirmed positives, 27 suppressed positives, 4 false-positive candidates, and 0 false-positive-positive rows.
- Ran the 30-episode official `episode_start` confirmation stress; it completed with 210 rows, 6 target-visible episodes, 27 target-visible rows, 4 confirmed positives, 20 suppressed positives, 14 false-positive candidates, and 0 false-positive-positive rows.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests -q`; 63 tests passed with one existing `pythonpath` config warning.
- Ran `git diff --check`; command exited successfully.
- Ran `git check-ignore -v` on the two new run summaries and confirmed they are ignored by `runs/**`.
- Ran `rg -n "041027|password|密码" README.md docs src || true`; no matches were found.

Effect:
- The current semantic memory runner no longer accepts isolated one-frame positive masks by default.
- The official start pass shows that scripted actions see the target in only 6 of 30 episodes and 27 of 210 rows, so full ObjectNav evaluation needs a navigation policy rather than static scripted sweeps.
- The stricter confirmation gate reduces false positives but also lowers final memory confidence when real target views are sparse.

Follow-up:
- Add debug PNG export for suppressed false-positive candidates and missed visible targets.
- Tune confirmation thresholds adaptively by category, mask area, range, and motion.
- Replace corrupted oracle masks with real detector replay after debug export is available.

## 2026-05-27 — Decision: publication target is robotics systems venues, no benchmark grinding

Files changed:
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md` (new)
- `docs/decisions/2026-05-27-publication-target-robotics-systems-no-benchmark-grinding.md` (new)

Change summary:
- Reviewed the four 2026-05-26..27 Habitat experiment reports and audited
  `objectnav_core/cli` + `objectnav_core/evaluation` runners. Confirmed
  that all current Habitat work corrupts the oracle semantic mask (not the
  RGB), runs a fixed 6-step action sequence, never revisits the target,
  never persists memory across episodes, and does not exercise
  `objectnav_core/planning/`.
- Wrote a design doc proposing an RGB-noise + real open-vocab detector +
  revisit controller + lifelong memory harness pipeline as the sim-to-real
  validation path, with a 6-cell experiment matrix
  (`clean/mild/heavy` × memory `on/off`) and custom memory metrics.
- Recorded a decision to target **ICRA / IROS / RA-L / CoRL** and to
  explicitly **not** push for HM3D ObjectNav leaderboard numbers, because
  the single-episode SPL/success protocol does not measure cross-episode
  memory reuse, which is the central contribution. The design doc above is
  the planned validation strategy under this decision.

Reason:
- The 0.909 → 0.757 → 0.683 → 0.626 drop across the four reports is not an
  algorithm regression; it is the test setup exposing harder categories,
  stricter confirmation, and (under `episode_start`) target invisibility.
  Continuing to push on the current mask-corruption stress does not move
  the algorithm toward the user's real goal (deployment on a real robot
  after sim validation).
- Vision-venue benchmark grinding would require integrating a navigation
  policy and re-running full HM3D val with reimplemented baselines, at
  2–3× the engineering cost, while still hiding the lifelong contribution
  inside single-episode SPL.

Verification:
- None executed in this entry. The decision and design are paper artifacts
  only. Verification will be performed when the design is implemented:
  unit tests for the noise pipeline, a Habitat smoke run, and the
  6-cell experiment matrix described in the design doc's Verification Plan.

Effect on future work:
- Next implementation work: `src/objectnav_core/objectnav_core/simulation/rgb_noise.py`,
  a detector adapter, a revisit controller, and a lifelong memory harness,
  wired into a new CLI `run_habitat_objectnav_rgb_noise_stress`.
- The existing mask-corruption runners stay as unit-test scaffolding for
  `UsabilityUpdater`, but are no longer the headline experiments.
- The Open Questions section of the design doc lists 6 items requiring
  user input before implementation starts (detector choice, noise-profile
  provenance, revisit strategy, depth noise in v1, memory persistence
  backend, oracle-stop acceptability).

## 2026-05-27 (later) — Finalize RGB-noise sim-to-real design doc

Files changed:
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
  - Status `Draft → Approved`.
  - Added a "Locked Decisions" table at the top (detector, RGB noise model,
    depth noise model, noise levels, profile provenance, memory
    persistence, revisit strategy, success metric).
  - Concretized System Boundary and Python interfaces with the chosen
    backends (YOLO-World via Ultralytics, RealSense D435 depth noise,
    sqlite_store-backed lifelong harness).
  - Updated the CLI sketch to take separate `--rgb-noise-profile` and
    `--depth-noise-profile` arguments and a `yolo_world` detector flag.
  - Added a full "Noise Model Specification" section with:
    - Foi 2008 Poisson-Gaussian formula and `a, b` levels.
    - Brooks 2019 motion-blur PSF tied to Habitat per-step motion and
      exposure time.
    - JPEG quality levels.
    - Nguyen 2012 axial + lateral depth noise and D435-tuned `α, β` levels.
    - Hole-dropping for D435 out-of-range / dropout pixels.
    - YAML profile examples for `rgb_published_v1.yaml` and
      `depth_realsense_d435_v1.yaml`, both tagged
      `provenance: published_model`.
  - Split Open Questions into a "Resolved 2026-05-27" table and a "Still
    open" list (YOLO-World prompt strategy, mask source, optional D435
    bag, optional `extreme` noise level).

Reason:
- User confirmed: target real camera is **Intel RealSense D435**, detector
  is **YOLO-World**, noise profile sources are **published models** (Foi
  2008, Brooks 2019, Nguyen 2012). All other open questions could be
  resolved without further user input under the existing publication-
  target decision.
- Locking these decisions in the design doc removes the need for a
  separate decision record per item, and lets implementation start
  directly against concrete formulas and configs.

Verification:
- No code executed. Document-only change. Verification of the noise
  pipeline itself is described in the design doc's Verification Plan and
  will run when implementation lands.

Effect on future work:
- Next implementation step is now unambiguous:
  1. `src/objectnav_core/objectnav_core/simulation/rgb_noise.py` (Foi +
     Brooks + JPEG).
  2. `src/objectnav_core/objectnav_core/simulation/depth_noise.py` (Nguyen
     D435).
  3. `configs/noise/rgb_published_v1.yaml`,
     `configs/noise/depth_realsense_d435_v1.yaml`.
  4. `src/objectnav_core/objectnav_core/perception/yolo_world_adapter.py`.
  5. `tests/test_rgb_noise.py`, `tests/test_depth_noise.py`,
     `tests/test_yolo_world_adapter.py`.
- Remaining "Still open" items are smoke-time decisions, not blockers.

## 2026-05-27 - Implement RGB-Noise Sim-to-Real Validation Base

File:
- `README.md`
- `docs/README.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `docs/devlog/2026-05.md`
- `configs/noise/rgb_published_v1.yaml`
- `configs/noise/depth_realsense_d435_v1.yaml`
- `src/objectnav_core/setup.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/lifelong_memory_harness.py`
- `src/objectnav_core/objectnav_core/perception/__init__.py`
- `src/objectnav_core/objectnav_core/perception/yolo_world_adapter.py`
- `src/objectnav_core/objectnav_core/simulation/depth_noise.py`
- `src/objectnav_core/objectnav_core/simulation/revisit_controller.py`
- `src/objectnav_core/objectnav_core/simulation/rgb_noise.py`
- `src/objectnav_core/tests/test_depth_noise.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_lifelong_memory_harness.py`
- `src/objectnav_core/tests/test_revisit_controller.py`
- `src/objectnav_core/tests/test_rgb_noise.py`
- `src/objectnav_core/tests/test_yolo_world_adapter.py`
- `src/objectnav_core/tests/test_ros_packaging.py`

Change:
- Added published-model RGB and RealSense D435 depth noise YAML profiles.
- Added deterministic RGB noise (`motion_blur -> poisson_gaussian -> jpeg`) and D435 depth noise (`axial -> lateral -> holes`) pipelines.
- Added a lazy YOLO-World detector adapter that turns detections into bbox masks while keeping `ultralytics` out of normal imports.
- Added an out-and-back Habitat action helper and SQLite-backed lifelong usability-belief persistence.
- Added `run_habitat_objectnav_rgb_noise_stress`, with a preflight mode and a full Habitat v1 path supporting `oracle_bbox` smoke and `yolo_world` detector modes.
- Updated README/docs index and added a handoff for the Linux run.

Reason:
- The user redirected the project away from oracle semantic-mask corruption and toward the approved sim-to-real validation design: realistic RGB/depth noise, real detector output, revisit behavior, and memory on/off ablation.

Verification:
- Wrote failing tests first; initial focused run failed with missing-module import errors.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py -q`; 11 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 4 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_ros_packaging.py -q`; 15 tests passed.

Effect:
- The repository now has a concrete, test-covered base for the RGB-noise sim-to-real experiment instead of only a design document.
- Linux Habitat can run an `oracle_bbox` smoke before the heavier YOLO-World dependency is installed or downloaded.

Follow-up:
- Run the new focused tests and a 1-episode `oracle_bbox` Habitat smoke on `badger@100.88.131.52` in `conda habitat`.
- Install/verify `ultralytics` and run a 1-episode YOLO-World smoke.
- Replace the deterministic out-and-back action helper with a navmesh-aware `ShortestPathFollower` revisit controller if the smoke exposes poor target revisit coverage.

## 2026-05-27 - Run RGB-Noise Oracle-BBox Smoke On Linux Habitat

File:
- `docs/experiments/2026-05-27-rgb-noise-oracle-bbox-smoke.md`
- `docs/README.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Pulled commit `f608c63` on `badger-linux`.
- Installed `pytest` into the `conda habitat` environment because it was missing.
- Ran the new focused tests, preflight CLI, and a 1-episode Habitat `oracle_bbox` smoke.
- Recorded the Linux result as an experiment report and updated the handoff.

Reason:
- The user asked to push the macOS implementation, pull it on the Linux Habitat machine, and run it there.
- The `oracle_bbox` smoke validates the new Habitat/noise/memory wiring before installing the heavier YOLO-World stack.

Verification:
- Ran focused tests in `conda habitat`; 15 passed.
- Ran preflight CLI; it wrote `runs/habitat_usability/rgb_noise_preflight_linux/summary.json`.
- Ran `detector=oracle_bbox`, `noise_levels=clean`, `memory_ablation=on`, `max_episodes=1`, `sensor_size=64`; it completed with 15 trace rows.
- Smoke metrics: evidence counts `positive=14`, `unknown=1`; decision counts `TRUST=12`, `VERIFY=3`; target-visible rows `15`; oracle-stop success rows `12`; mean final `p_valid=0.999599`.
- Attempted full test-suite verification in `conda habitat`; it failed during collection because that env is Python 3.9 while the repository requires Python `>=3.13`, and full tests also need `pydantic`.
- Checked `ultralytics`/`torch` availability; both are missing in `conda habitat`, so YOLO-World was not run.

Effect:
- The RGB-noise validation path is now verified on the actual Linux Habitat machine for a minimal clean wiring smoke.
- The next blocker is detector environment setup, not Habitat scene loading or memory plumbing.

Follow-up:
- Install a compatible `ultralytics` / PyTorch stack in `conda habitat`.
- Run a 1-episode YOLO-World clean smoke.
- Then run the 6-cell `clean/mild/heavy × memory on/off` matrix.

## 2026-05-27 - Install YOLO-World Stack And Run Clean Detector Smoke

File:
- `docs/experiments/2026-05-27-yolo-world-clean-smoke.md`
- `docs/README.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Installed PyTorch CUDA and Ultralytics into the Linux `conda habitat` environment.
- Initialized YOLO-World with `yolov8s-worldv2.pt`, which downloaded the model and installed the Ultralytics CLIP dependency.
- Ran focused detector tests and two 1-episode clean Habitat smokes with `detector=yolo_world` at sensor sizes 64 and 96.
- Recorded the environment and smoke results.

Reason:
- The user asked to SSH into `badger@100.88.131.52` and install the corresponding detector environment.
- The RGB-noise validation design requires a real detector path before the memory/noise matrix can be meaningful.

Verification:
- Verified `torch==2.8.0+cu128`, `torchvision==0.23.0+cu128`, `ultralytics==8.4.56`, `clip==1.0`.
- Verified `torch.cuda.is_available()` is true and the CUDA device is `NVIDIA GeForce RTX 4070 Laptop GPU`.
- Verified `habitat_sim` and `habitat` still import at version 0.3.3.
- Ran `python -m pip check`; no broken requirements found.
- Ran focused tests `test_habitat_objectnav_rgb_noise_stress.py` and `test_yolo_world_adapter.py`; 5 passed.
- Ran YOLO-World clean smokes at sensor sizes 64 and 96; both completed with 15 trace rows, target-visible rows `15`, detector recall `0.0`, and final `p_valid=0.011171`.

Effect:
- The detector environment is ready, but the first real-detector smoke exposes a perception failure rather than a memory failure.
- The memory policy correctly avoids trust when YOLO-World misses a visible target repeatedly.

Follow-up:
- Add debug PNG export for clean RGB / GT mask / detector output.
- Try expanded YOLO-World prompts and higher sensor sizes.
- Run a small category sweep before the full noise-memory matrix.

## 2026-05-27 - Debug YOLO-World Toilet Misses

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Added explicit YOLO-World prompt modes: `target`, `all_categories`, and `target_aliases`.
- Changed the RGB-noise ObjectNav harness default from full-category YOLO prompting to target-conditioned prompting.
- Raised the default Habitat RGB sensor size for this harness from 96 px to 320 px.
- Added trace metadata for the detector prompt categories used in each row.

Reason:
- The first YOLO-World smoke missed the visible `toilet` target, blocking real-detector validation.
- Debugging showed that 64/96 px frames were too small, while 320 px frames detected early target views but the full 6-category prompt set let `bed` outcompete `toilet` on several visible frames.
- ObjectNav knows the current goal category, so target-conditioned detector prompting better matches the task interface.

Verification:
- Added failing tests for the new defaults and prompt-mode validation.
- Ran `PYTHONPATH=src/objectnav_core pytest -q src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_yolo_world_adapter.py`; 7 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_yolo_world_adapter.py -q`; 7 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 17 tests passed.
- Pulled commit `28b01b9` on `badger-linux` and ran focused tests in `conda habitat`; 7 tests passed.
- Ran the target-conditioned clean Habitat smoke at the new defaults. It completed with `sensor_size=320`, `yolo_prompt_mode=target`, `positive=4`, `non_confirmation=10`, `unknown=1`, mean oracle recall `0.333309`, and oracle-stop success rows `1`.

Effect:
- The default smoke path now exercises a detector configuration that can confirm the target when it is rendered at usable resolution.
- The legacy all-category prompt mode remains available for controlled ablations.

Follow-up:
- Run a category sweep before the full 6-cell noise-memory matrix.
- Improve revisit sampling so the detector sees full-object target views instead of mostly edge-clipped fragments.

## 2026-05-27 - Add ObjectNav Stop Semantics To RGB-Noise Harness

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Added `stop_on_trust`, defaulting to `true`, so an episode ends once the policy chooses `TRUST` while the target is visible.
- Added `--stop-on-trust` / `--no-stop-on-trust` CLI support.
- Added per-row oracle target view metrics: `oracle_bbox`, `oracle_bbox_area`, `oracle_bbox_fill_ratio`, `oracle_touches_edge`, `oracle_touches_side_edge`, `oracle_edge_sides`, and `oracle_edge_clearance_ratio`.

Reason:
- The target-conditioned YOLO smoke reached `TRUST` at step 5, then the runner kept walking through edge-clipped target fragments and decayed the belief. That continuation is a harness artifact, not ObjectNav STOP behavior.
- The new view metrics make later edge-clipped misses visible in the trace instead of mixing them with true detector failures.

Verification:
- Added failing tests for the new default, stop helper, and edge-clipped view metrics.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`; 7 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 19 tests passed.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_habitat_objectnav_rgb_noise_stress --output /tmp/rgb_noise_preflight_stop --noise-levels clean --detector yolo_world --memory-ablation on --max-episodes 1 --preflight-only`; summary reported `stop_on_trust=true`.
- Ran `git diff --check`; no whitespace errors.
- Extended the edge metric test to distinguish floor/bottom contact from left/right side clipping, then reran the focused and combined test commands; 7 and 19 tests passed respectively.

Effect:
- The harness now reports ObjectNav-style success without artificially continuing past the first valid stop.
- Full-trace debugging remains available with `--no-stop-on-trust`.

Follow-up:
- Pull and run the target-conditioned Habitat smoke on `badger-linux`.
- Then run a small category sweep with stop semantics enabled.

## 2026-05-27 - Write YOLO-World ObjectNav HTML Status Report

File:
- `docs/experiments/2026-05-27-yolo-world-objectnav-status-report.zh.html`
- `docs/README.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Added a detailed Chinese HTML report covering the YOLO-World Habitat debug work, experiment metrics, algorithm status, current risks, and next recommended actions.
- Linked the report from the documentation index.
- Updated the handoff with the stop-on-trust smoke, 6-episode category sweep, alias sweep, and plant prompt probe conclusions.

Reason:
- The user asked for a detailed HTML document before sleeping the Linux machine, so the completed remote results needed to be preserved in a locally readable artifact.

Verification:
- Linux was no longer needed for further work; the final attempted SSH command timed out, so no additional remote jobs were started.
- Report content was generated locally from already completed command outputs and committed experiment notes.

Effect:
- A future contributor can resume from the HTML report without reading the full chat transcript.
- The current state is explicit: toilet works under target prompt + 320 px + stop-on-trust; plant remains the next detector bottleneck.

Follow-up:
- Verify the HTML file locally after creation.
- Commit and push the documentation update.

## 2026-05-28 - Add Real-Resolution Detector Qualification Controls

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_semantic_yolo_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Added rectangular sensor resolution support through `--sensor-width` and `--sensor-height`, with `--sensor-size` kept as a square override for old smoke commands.
- Set the non-square default to `640x480`, and prepared the detector qualification command to run at `1280x720`.
- Added balanced category selection through `--target-categories` and `--episodes-per-category`.
- Added summary fields for `sensor_width`, `sensor_height`, `sensor_resolution`, `category_filter`, `episodes_per_category`, and episode category counts.

Reason:
- The user asked to stop using low smoke-test resolution and start detector category qualification at a normal camera-like resolution.
- The previous `max_episodes` path followed dataset order and over-sampled early categories, so it could not fairly qualify all ObjectNav categories.

Verification:
- Wrote failing tests first for rectangular resolution defaults, square override behavior, and balanced category selection.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`; 9 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_habitat_objectnav_rgb_noise_stress --output /tmp/rgb_noise_preflight_rect --noise-levels clean --detector yolo_world --memory-ablation on --episodes-per-category 1 --sensor-width 1280 --sensor-height 720 --preflight-only`; summary reported `sensor_resolution=1280x720`.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 29 tests passed.
- Ran `git diff --check`; no whitespace errors.

Effect:
- The harness can now run clean detector category qualification with balanced categories at camera-like resolution.
- Existing square smoke commands remain available with `--sensor-size`.

Follow-up:
- Push to GitHub, pull on `badger-linux`, and run the first `1280x720` detector category qualification pass.

## 2026-05-28 - Run 1280x720 Detector Category Qualification

File:
- `docs/experiments/2026-05-28-detector-category-qualification-1280x720.md`
- `docs/experiments/2026-05-28-detector-category-qualification-1280x720.zh.html`
- `docs/README.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Pulled commit `18254e7` on `badger-linux`.
- Ran focused tests and preflight in the `conda habitat` environment.
- Ran the first `1280x720` detector category qualification pass with one episode per category and stop-on-trust.
- Ran a full-trace two-episode-per-category qualification pass with `--no-stop-on-trust`.
- Ran an additional chair/tv-monitor probe because early chair episodes had no visible target rows.
- Recorded the results in a dedicated experiment report.
- Added a Chinese HTML report that explains the work, results, current state, and recommended next steps.

Reason:
- The user asked to simulate normal camera-like resolution and start detector category qualification before the full noise-memory matrix.

Verification:
- Remote focused tests passed: 11 tests.
- Remote preflight reported `sensor_resolution=1280x720` and `episodes_per_category=1`.
- Qualification `epc1` completed 6 episodes and produced 3 oracle-stop successes (`bed`, `sofa`, `toilet`).
- Qualification `epc2_fulltrace` completed 12 episodes and 180 trace rows.
- Chair/tv-monitor probe completed 10 episodes and confirmed no positive rows under current sampling/detector setup.
- Inspected the HTML report locally and checked that the report links are present in `docs/README.md`.

Effect:
- Detector-ready first-pass categories: `bed`, `sofa`, `toilet`.
- Detector blockers: `plant`, `tv_monitor`.
- Sampling/visibility blocker: `chair`.
- The full noise-memory matrix should not be run across all categories yet.

Follow-up:
- Add visibility-aware episode selection for detector qualification.
- Generate debug PNGs for plant, tv-monitor, and chair failure cases.
- Run the first noise-memory matrix only on detector-ready categories unless blockers are fixed.

## 2026-05-28 - Add Grounding-DINO Detector Backend

File:
- `src/objectnav_core/objectnav_core/perception/grounding_dino_adapter.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_grounding_dino_adapter.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`

Change:
- Added a Grounding-DINO detector adapter using Hugging Face Transformers'
  `AutoProcessor` / `AutoModelForZeroShotObjectDetection` path.
- Kept the detector output contract identical to YOLO-World:
  `(category, bbox, confidence, mask)` with v1 masks derived from boxes.
- Added `grounding_dino` to the RGB-noise ObjectNav harness detector choices.
- Added `--grounding-dino-text-threshold` and allowed
  `--detector-weights IDEA-Research/grounding-dino-tiny`.
- Updated the RGB-noise validation design doc to make Grounding-DINO a detector
  comparison that does not change memory, policy, noise, or summary semantics.

Reason:
- The user asked to replace YOLO-World with Grounding-DINO and rerun the
  `1280x720` detector category qualification to see whether all six categories
  become detector-ready.

Verification:
- Wrote failing tests first for the missing Grounding-DINO adapter and harness
  preflight support.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_grounding_dino_adapter.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`; 14 tests passed after implementation.
- Ran `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_grounding_dino_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py src/objectnav_core/tests/test_ros_packaging.py -q`; 34 tests passed.
- Ran Grounding-DINO preflight at `1280x720`; summary reported
  `detector=grounding_dino`, `grounding_dino_text_threshold=0.2`, and
  `sensor_resolution=1280x720`.
- Ran `git diff --check`; no whitespace errors.

Effect:
- The Linux Habitat environment can now run the same clean detector category
  qualification protocol with Grounding-DINO once Transformers dependencies and
  model weights are available.

Follow-up:
- Pull this commit on `badger-linux`.
- Install Transformers/Grounding-DINO runtime dependencies in `conda habitat`.
- Run the `1280x720` Grounding-DINO category qualification and compare category
  readiness against the YOLO-World report.

## 2026-05-28 - Run Grounding-DINO Category Qualification

File:
- `docs/experiments/2026-05-28-grounding-dino-category-qualification-1280x720.md`
- `docs/README.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Installed Transformers runtime dependencies in `conda habitat`.
- Ran Grounding-DINO model-load and adapter smokes on `badger-linux`.
- Fixed Transformers API compatibility and added detector-side inference resize
  support after uncapped/large-cap `1280x720` runs hit CUDA OOM.
- Ran clean `1280x720` Grounding-DINO category qualification with
  `grounding_dino_max_image_side=384`.
- Ran a chair-only probe to separate detector performance from episode
  visibility.
- Recorded the results in a dedicated experiment report.

Reason:
- The user asked to switch the detector to Grounding-DINO and rerun detector
  category qualification to see whether all six target categories become ready.

Verification:
- Remote focused tests passed after pulling the Grounding-DINO implementation.
- Grounding-DINO model smoke loaded `IDEA-Research/grounding-dino-tiny` on CUDA
  and produced detections.
- Preflight reported `detector=grounding_dino`, `sensor_resolution=1280x720`,
  and `grounding_dino_text_threshold=0.25`.
- The first uncapped and `640/512` cap attempts failed with CUDA OOM; `384`,
  `320`, and `256` detector-side caps ran on a synthetic `1280x720` input.
- Stop-on-trust qualification completed 6 episodes and 57 trace rows.
- Full-trace qualification completed 12 episodes and 180 trace rows.
- Chair probe completed 7 episodes and 105 trace rows.

Effect:
- Grounding-DINO improves category qualification materially:
  `bed`, `sofa`, `toilet`, and `plant` are ready in the main full-trace pass.
- `tv_monitor` is now usable but sparse-view sensitive.
- `chair` is not ready under first-N episode ordering, but later sparse-visible
  chair probes produce success rows, so visibility-aware sampling is now the
  main blocker.

Follow-up:
- Implement visibility-aware category qualification before claiming all six
  categories are ready.
- Re-run Grounding-DINO with visible samples per category.
- Use `grounding_dino_max_image_side=384` on the RTX 4070 Laptop GPU.

## 2026-05-28 - Run Memory-Only Grounding-DINO Replay Matrix

File:
- `docs/experiments/2026-05-28-memory-only-grounding-dino-replay-1280x720.md`
- `docs/experiments/2026-05-28-memory-only-grounding-dino-replay-1280x720.zh.html`
- `docs/README.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Ran the first memory-only / replay-style Habitat matrix on `badger-linux`
  with fixed `out_and_back` trajectories, Grounding-DINO, RGB/depth noise, and
  memory `on/off`.
- Used `1280x720` Habitat RGB/depth observations and detector-side
  `grounding_dino_max_image_side=384`.
- Restricted the matrix to detector-qualified or partially usable categories:
  `bed`, `sofa`, `toilet`, `plant`, and `tv_monitor`.
- Excluded `chair` because the previous qualification showed first-N chair
  samples were blocked by oracle target visibility.
- Aggregated memory/noise/category/revisit-proxy metrics and wrote a Markdown
  experiment report plus a Chinese HTML status report.

Reason:
- The user asked how to validate the memory system without a navigation system,
  then approved a memory-only replay simulation with fixed trajectory,
  Grounding-DINO, RGB/depth noise, and memory `on/off`.

Verification:
- Remote preflight reported `detector=grounding_dino`,
  `sensor_resolution=1280x720`, `noise_levels=clean,mild,heavy`,
  `memory_ablation=on,off`, and `stop_on_trust=false`.
- Remote main run completed 60 replay-runs and wrote 900 trace rows to
  `runs/habitat_usability/memory_only_grounding_dino_replay_1280x720_epc2_cap384`.
- Remote artifact check confirmed `summary.json` and `rgb_noise_trace.csv`
  exist and summary fields report `60 900 268 1280x720 grounding_dino 384`.
- Local aggregation from the trace found memory `on` produced 372 trust rows
  and 268 oracle-stop success rows, while memory `off` produced zero trust rows
  and zero success rows.

Effect:
- The replay harness now provides evidence that the memory update and decision
  path turns repeated detector evidence into trust on fixed trajectories.
- The strongest current categories are `bed`, `sofa`, and `toilet`.
- `plant` and `tv_monitor` expose false-trust / sparse-visibility risk and
  need stronger current-view or geometric stop gating.

Follow-up:
- Add an `episode_local` ablation to separate within-episode accumulation from
  cross-episode lifelong memory.
- Add a current-view confirmation gate for stop/trust decisions.
- Implement visibility-aware episode selection before reintroducing `chair`.

## 2026-05-28 - Add Naive-Count Baseline And Shared Trust Gate

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added `memory_ablation=naive_count` as a non-memory accumulating baseline.
- Kept `naive_count` deliberately simple: it only counts positive evidence
  inside the current replay; non-confirmation and unknown observations do not
  affect it, and the first positive observation immediately creates the naive
  belief instead of using delayed birth.
- Added a shared decision-side current-positive gate for all memory modes:
  raw `TRUST` is allowed to remain `TRUST` only when the current frame has both
  oracle target visibility and positive detector evidence.
- Wrote `raw_decision`, gated `decision`, `decision_gate_reason`, and
  `naive_positive_count` to the trace so false-trust can be measured directly.
- Added `raw_decision_counts` and `decision_gate_reason_counts` to run
  summaries.

Reason:
- The previous `off` baseline was a strawman single-frame sanity check and did
  not accumulate evidence.
- `plant` and `tv_monitor` false-trust came from allowing historical memory
  confidence to act as a stop decision without current-frame confirmation.
- The user clarified that the baseline must not borrow non-confirmation,
  unknown handling, or delayed birth from the proposed algorithm, and that the
  trust gate must be shared by all three modes rather than only changing
  metrics or only benefiting memory `on`.

Verification:
- Wrote failing tests for accepting `naive_count`, positive-only naive
  accumulation, shared current-positive decision gating, and summary reporting
  of raw/gated decision counts.
- Ran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  16 tests passed after implementation.
- Ran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_rgb_noise.py src/objectnav_core/tests/test_depth_noise.py src/objectnav_core/tests/test_yolo_world_adapter.py src/objectnav_core/tests/test_grounding_dino_adapter.py src/objectnav_core/tests/test_revisit_controller.py src/objectnav_core/tests/test_lifelong_memory_harness.py src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_habitat_semantic_yolo_stress.py src/objectnav_core/tests/test_usability_memory.py src/objectnav_core/tests/test_ros_packaging.py -q`;
  50 tests passed.
- Ran preflight with `--memory-ablation on,naive_count,off`; summary included
  `naive_count`, `sensor_resolution=1280x720`, and `stop_on_trust=false`.

Effect:
- The next replay experiment can compare memory `on` against a fairer
  accumulating-but-naive baseline.
- False-trust is now visible as raw `TRUST` rejected by the shared decision
  gate, instead of being hidden behind post-hoc metric changes.

Follow-up:
- Pull on `badger-linux` and run focused tests in `conda habitat`.
- Run a small Grounding-DINO replay smoke with `on,naive_count,off`.
- Re-run the full memory-only replay matrix and update the experiment report.

## 2026-05-28 - Correct Naive-Count Trust Threshold

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Corrected `naive_count` so the first positive observation increments the
  count but does not cross the trust threshold.
- Set the naive trust threshold to two positive observations.
- Kept the baseline positive-only: non-confirmation and unknown observations
  do not affect the count or belief.

Reason:
- The first implementation let `naive_count` trust after a single positive
  observation, which made it behave like a single-frame baseline and undermined
  its role as an accumulating comparison.

Verification:
- Updated the regression test so one positive remains below
  `NAIVE_COUNT_TRUST_P_VALID`, non-confirmation and unknown do not change the
  count, and two positives cross the trust threshold.
- Ran the focused regression locally; it passed.

Effect:
- `naive_count` is now a minimal accumulating positive-count baseline rather
  than a single-frame strawman.

## 2026-05-28 - Re-run Replay Matrix With Naive Count And Shared Gate

File:
- `docs/experiments/2026-05-28-memory-only-grounding-dino-replay-1280x720.md`
- `docs/experiments/2026-05-28-memory-only-grounding-dino-replay-1280x720.zh.html`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Re-ran the full memory-only / replay-style Habitat matrix on `badger-linux`
  after adding `naive_count` and the shared current-positive decision gate.
- Used Grounding-DINO at `1280x720` with detector-side
  `grounding_dino_max_image_side=384`.
- Ran `memory_ablation=on,naive_count,off` across
  `clean,mild,heavy` and categories `bed,sofa,toilet,plant,tv_monitor`.
- Updated the Markdown and Chinese HTML experiment reports with the new
  three-way results.

Reason:
- The previous report compared memory `on` against a strawman `off` baseline
  and counted raw trust without a shared decision-side gate.

Verification:
- Remote preflight reported `memory_ablation=on,naive_count,off`,
  `sensor_resolution=1280x720`, and `stop_on_trust=false`.
- Remote main run completed 90 replay-runs and 1350 trace rows.
- Summary reported 694 raw trust rows, 488 gated trust / success rows, and 206
  gate rejections.
- Local aggregation from the trace confirmed:
  - `on`: 372 raw trust, 246 gated success, 126 gate rejections
  - `naive_count`: 322 raw trust, 242 gated success, 80 gate rejections
  - `off`: 0 trust and 0 success

Effect:
- The shared gate materially reduces sparse-category false-trust reporting,
  especially for `tv_monitor`.
- The run supports "positive evidence accumulation helps" but does not yet show
  that Dual-Anchor / Lifelong memory beats the naive positive-count baseline.

Follow-up:
- Add a task where naive count should fail: cross-episode persistence,
  scene-change handling, negative evidence, or geometry consistency.
- Generate debug PNGs for `plant` and `tv_monitor` gate rejections.

## 2026-05-28 - Add Gate-Rejection Debug PNG Export

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added optional `--debug-export-gate-rejections` support to the RGB-noise
  replay runner.
- Added category and per-category-limit controls for debug export, defaulting
  to `plant,tv_monitor` and 256 PNGs per category.
- For each exported gate rejection, the runner writes a side-by-side PNG with
  clean RGB, noisy RGB with detector boxes, Habitat GT mask, detector box-mask,
  combined GT/detector overlap, and frame metadata.
- Added `debug_png` to trace rows and debug PNG count fields to the summary.

Reason:
- The `plant` and `tv_monitor` gate rejections need visual diagnosis. Metrics
  alone cannot distinguish Grounding-DINO false boxes from Habitat semantic GT
  masks that are too sparse or strict.

Verification:
- Added tests for preflight debug config, gate-rejection export filtering, and
  PNG artifact generation.
- Ran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  19 tests passed.
- Ran the broader focused suite; 53 tests passed.
- Ran CLI preflight with `--debug-export-gate-rejections`; summary reported
  `debug_export_gate_rejections=true`, `debug_export_categories=plant,tv_monitor`,
  and `debug_export_limit_per_category=7`.

Effect:
- The next Linux replay can export the rejected `plant` and `tv_monitor` frames
  as PNGs for manual review without changing the experiment metrics.

## 2026-05-28 - Export And Inspect Plant/TV Gate-Rejection PNGs

File:
- `docs/README.md`
- `docs/experiments/2026-05-28-gate-rejection-debug-pngs.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Ran the `plant,tv_monitor` replay subset on `badger-linux` with
  `--debug-export-gate-rejections`.
- Exported all rejected raw-`TRUST` frames to PNG under the run directory.
- Copied four representative PNGs to `/tmp/dual_anchor_gate_debug_samples/`
  and generated a local contact sheet for quick visual review.
- Wrote a diagnostic experiment report with the rejection breakdown and
  representative trace rows.
- Linked the report from the documentation index.

Reason:
- The user asked to export the rejected `tv_monitor` and `plant` frames as PNGs
  and inspect whether the failures came from Grounding-DINO false boxes or
  Habitat GT mask strictness.

Verification:
- Pulled commit `97754ab` on `badger-linux`.
- Remote focused tests passed: 19 tests.
- Remote preflight confirmed `debug_export_gate_rejections=true`,
  `debug_export_categories=plant,tv_monitor`, and
  `sensor_resolution=1280x720`.
- Main run completed 24 replay-runs and 360 trace rows.
- Summary reported `debug_png_counts={"plant": 23, "tv_monitor": 154}` and no
  skipped debug PNGs.
- Local visual inspection of the contact sheet confirmed the representative
  `tv_monitor` false-positive and mixed `plant` strict-GT / coarse-box cases.

Effect:
- `tv_monitor` looks primarily like Grounding-DINO false-positive /
  over-broad-box behavior, especially in out-of-view frames.
- `plant` is mixed: some out-of-view detector false positives, plus visible
  edge cases where Habitat GT is much smaller than the detector box.

## 2026-05-28 - Add Shared Detector Area Filter

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added a shared detector-side maximum area filter before detections enter the
  evidence classifier.
- The filter rejects individual over-broad boxes and also rejects the whole
  detector mask if the union of remaining boxes is still over-broad.
- Defaulted `max_detection_area_ratio` to `0.70`; CLI values `<=0` disable the
  filter for ablations.
- Added `detection_filtered_count` to trace rows and total filtered detection
  count to summaries.

Reason:
- The gate-rejection PNGs showed `tv_monitor` detections that covered most or
  all of the image while Habitat GT had zero target pixels. Those full-frame
  detector boxes should not become positive memory evidence.

Verification:
- Added failing tests for filtering over-broad detector boxes, dropping
  over-broad union masks, and preserving old behavior when the filter is
  disabled.
- Ran the focused RGB-noise stress tests; 22 tests passed.
- Ran the broader focused suite; 56 tests passed.
- Ran CLI preflight and confirmed `max_detection_area_ratio=0.7` is recorded.

Effect:
- The next Linux diagnostic replay can test whether rejecting pathological
  full-frame boxes materially reduces `tv_monitor` false positives without
  changing memory or gate logic.

Follow-up result:
- Linux reruns showed the conservative `0.70` filter reduced out-of-view
  positive rows from `158` to `128` while preserving all `36` visible positive
  rows and all `21` oracle-stop success rows.
- An aggressive `0.40` sweep reduced out-of-view positives further to `102`,
  but also removed all plant visible positives and cut oracle-stop success rows
  to `11`.
- Keep `0.70` as the default; stricter area thresholds are not a safe detector
  fix for `plant`.

## 2026-05-28 - Add Structured Naive-Count Decision Challenge

File:
- `src/objectnav_core/objectnav_core/evaluation/grid_trace_experiment.py`
- `src/objectnav_core/tests/test_grid_trace_experiment.py`
- `docs/design/2026-05-26-2d-grid-trace-generator.md`
- `docs/experiments/2026-05-28-structured-naive-count-decision-challenge.md`
- `docs/experiments/2026-05-28-structured-naive-count-decision-challenge.zh.html`
- `docs/README.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Extended the 2D grid trace replay to emit both `usability_memory` and
  `naive_count` policy decisions on the same events.
- Kept `naive_count` positive-only with a two-positive threshold; it does not
  consume non-confirmation, unknown, scene-change, delayed birth, JPDA, or
  geometry.
- Applied the same current-positive decision gate to both policies and wrote
  raw/gated decision columns to `events.csv`.
- Changed `removed_or_moved` to start with two positive observations before
  non-confirmation / scene-change evidence, so the baseline reaches trust
  honestly before stale evidence arrives.
- Added baseline comparison summaries and an HTML section for the generated
  trace report.
- Ran a local 900-episode structured challenge and documented the result.

Reason:
- The Habitat fixed replay was too friendly to positive counting. A structured
  room/corridor-style decision challenge is needed to show where memory,
  negative evidence, and association guards matter beyond `naive_count`.

Verification:
- Wrote failing tests for the changed `removed_or_moved` sequence and baseline
  comparison output, then implemented until they passed.
- Ran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_grid_trace_experiment.py -q`;
  3 tests passed.
- Ran the experiment command:
  `PYTHONPATH=src/objectnav_core python3 -m objectnav_core.cli.run_grid_trace_experiment --output runs/grid_trace/structured_naive_count_decision_challenge_seed17_ep900 --seed 17 --episodes 900 --steps-per-episode 8`.

Effect:
- `naive_count` produced `1241` unsafe raw-trust rows versus `882` for
  `usability_memory`.
- In `removed_or_moved`, `naive_count` produced `600` unsafe raw-trust rows
  after early positives, while `usability_memory` produced `7` and retired.
- In `multi_object_association`, `naive_count` showed `200` false-positive
  write-pressure rows while the JPDA-style memory path recorded zero.
- The next Habitat task should port this structure into a physical replay
  rather than continuing only with simple fixed-room trajectories.

Follow-up:
- Build or select Habitat routes with room/corridor revisits, target
  disappearance/reobservation, blocked access, stale cost refresh, and nearby
  same-class distractors.
- Keep the shared gate and positive-only `naive_count` semantics unchanged.

## 2026-05-28 - Add Structured Habitat Episode Selection

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `docs/experiments/2026-05-28-structured-naive-count-decision-challenge.md`

Change:
- Added `episode_selection_strategy` to the RGB-noise Habitat harness and CLI.
- Added `structured_visibility`, a metadata-only selection mode requiring a
  minimum number of goal viewpoints, minimum geodesic distance, and minimum
  geodesic/euclidean path-complexity ratio.
- Recorded structured selection thresholds in preflight summaries and full-run
  episode-selection summaries.
- Added unit tests for strategy configuration, category-order-preserving
  structured selection, and selection audit summaries.

Reason:
- The fixed replay matrix was too favorable to `naive_count`. The next Habitat
  run should start from episodes more likely to exercise room/corridor revisit
  structure instead of taking the first N category episodes blindly.

Verification:
- Wrote failing tests for the new preflight fields and structured episode
  selector, then implemented until the focused tests passed.
- Ran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  24 tests passed.
- Ran the combined focused regression:
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_grid_trace_experiment.py -q`;
  27 tests passed.
- Ran CLI preflight with `--episode-selection-strategy structured_visibility`
  and confirmed the structured thresholds were written to the summary.
- Ran compile and whitespace checks:
  `PYTHONPATH=src/objectnav_core python3 -m compileall -q src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
  and `git diff --check`.

Effect:
- Linux can now run Grounding-DINO replay with
  `--episode-selection-strategy structured_visibility` and inspect
  `summary.json["episode_selection"]` before treating the result as a memory
  claim.

Follow-up:
- Pull this commit on Linux and run a small structured Grounding-DINO replay
  before expanding to the full noise matrix.

## 2026-05-28 - Run Structured-Visibility Grounding-DINO Replay

File:
- `docs/experiments/2026-05-28-structured-visibility-grounding-dino-replay.md`
- `docs/README.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`

Change:
- Pulled the structured episode-selection implementation on `badger-linux`.
- Fixed the RGB-noise test module so it imports under the Linux Python 3.9
  `conda habitat` environment.
- Ran the structured-visibility Grounding-DINO replay at `1280x720` with
  `on,naive_count,off` and `clean,mild,heavy`.
- Wrote an experiment report with episode-selection audit and memory metrics.

Reason:
- The next decision was to inspect `summary.json["episode_selection"]` before
  trusting memory-vs-baseline metrics.

Verification:
- Remote `git pull --ff-only` updated Linux to `22cacd2`.
- Remote focused test passed in `conda habitat`: 24 tests.
- Run completed and wrote:
  `runs/habitat_usability/structured_visibility_grounding_dino_replay_1280x720_epc2_cap384/summary.json`
  and `rgb_noise_trace.csv`.
- Parsed `summary.json`, `rgb_noise_trace.csv`, and a metadata audit of
  selected categories.

Effect:
- `structured_visibility` selected 6 episodes from 9 structured candidates:
  `bed=2`, `toilet=2`, `plant=2`.
- It dropped `sofa` and `tv_monitor` because the current threshold found zero
  structured candidates for those categories.
- `naive_count` still slightly beat memory `on` on gated trust / success
  (`132` vs `123`), so this run is not evidence that memory beats the
  baseline.
- The result argues for an explicit structured replay protocol instead of
  relying on metadata selection alone.

Follow-up:
- Add fallback reporting for categories with no structured candidates.
- Implement explicit Habitat replay phases: confirm, depart, non-confirm, and
  revisit.

## 2026-05-28 - Add Replay Phase And Category Audit Fields

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added per-category selection audit fields to `summary.json`, including
  `category_audit` and `zero_structured_candidate_categories`.
- Added `replay_phase` to each RGB-noise trace row with the phases `confirm`,
  `depart`, `non_confirm`, and `revisit`.
- Added run-level `replay_phase_*` summaries for evidence, gated decisions,
  and raw decisions.

Reason:
- The structured-visibility replay dropped `sofa` and `tv_monitor` and still
  favored repeated positives. The next run needs an explicit audit showing
  which categories were excluded and whether the fixed trajectory actually
  produces non-confirmation and revisit evidence.

Verification:
- Wrote failing tests for zero-structured category reporting, replay phase
  partitioning, and phase-level summary counts.
- Confirmed the tests failed for the missing fields/functions.
- Implemented the audit fields and reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  26 tests passed.

Effect:
- Future Habitat replay results can be audited through
  `summary.json["episode_selection"]["category_audit"]` and
  `summary.json["replay_phase_evidence_counts"]` before comparing memory
  `on` with `naive_count`.

Follow-up:
- Pull this commit on Linux and rerun the structured-visibility
  Grounding-DINO replay. First inspect the new phase counts; only interpret
  memory metrics if the selected episodes include meaningful `non_confirm`
  and `revisit` evidence.

## 2026-05-28 - Run Phase-Audit Structured Replay

File:
- `docs/experiments/2026-05-28-structured-visibility-grounding-dino-replay.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Pulled commit `5b2a583` on `badger-linux`.
- Reran the structured-visibility Grounding-DINO replay into
  `runs/habitat_usability/structured_visibility_grounding_dino_replay_1280x720_epc2_cap384_phase_audit`.
- Updated the experiment report with `category_audit`,
  `zero_structured_candidate_categories`, and phase-level evidence/decision
  metrics.

Reason:
- The previous replay result showed `naive_count` still slightly ahead. The new
  audit fields were needed to determine whether the selected Habitat replay
  actually contained the non-confirmation and revisit structure required to
  test memory.

Verification:
- Remote `git pull --ff-only` updated Linux to `5b2a583`.
- Remote focused test passed in `conda habitat`: 26 tests.
- The phase-audit replay completed with 54 replay summaries and 810 trace
  rows.
- Parsed `summary.json` and `rgb_noise_trace.csv` from the run directory.

Effect:
- `sofa` and `tv_monitor` are now explicitly reported as
  `no_structured_candidates`.
- The run still selected `bed=2`, `toilet=2`, and `plant=2`.
- The phase audit showed `non_confirm` is not a true non-confirmation window:
  it has 216 rows, 198 target-visible rows, 108 positive rows, and only 90
  non-confirmation rows.
- `naive_count` remains ahead on gated trust / success (`132` vs `123`), so
  this remains a negative/diagnostic result rather than a memory-win claim.

Follow-up:
- Implement a real Habitat viewpoint/action protocol that intentionally moves
  from confirmed target visibility to target-not-visible/non-confirmation and
  then back to revisit, instead of relying on action-index phase labels.

## 2026-05-28 - Add Visibility-Challenge Replay Protocol

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added `--replay-protocol` with `out_and_back` as the default and
  `visibility_challenge` as an explicit memory stress protocol.
- Added replay view candidates and replay steps so the harness can choose a
  measured target-visible view for `confirm/revisit` and a measured
  target-hidden view for `depart/non_confirm`.
- Added trace fields `replay_protocol`, `replay_source`, and
  `replay_source_target_pixels`.
- Kept detector inference, evidence classification, memory updates,
  `naive_count`, and the shared current-positive gate unchanged.

Reason:
- The phase-audit replay proved the old out-and-back path did not create a
  true non-confirmation interval. The harness needs an explicit viewpoint
  protocol before memory-vs-`naive_count` metrics can become meaningful.

Verification:
- Wrote failing tests for the new preflight field and the visible/hidden
  replay-step builder.
- Confirmed the tests failed before implementation.
- Implemented the protocol and reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  29 tests passed.
- Ran CLI preflight with `--replay-protocol visibility_challenge`.
- Ran compile and whitespace checks.

Effect:
- The next Linux run can use `--replay-protocol visibility_challenge` to create
  a real target-hidden non-confirmation window instead of relying on action
  index labels.

Follow-up:
- Run a Linux `oracle_bbox` smoke first to verify the selected hidden views
  produce target-not-visible rows, then run a small Grounding-DINO memory
  comparison.

## 2026-05-28 - Run Visibility-Challenge Replay Smokes

File:
- `docs/experiments/2026-05-28-visibility-challenge-replay-smoke.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Pulled commit `df8cf45` on `badger-linux`.
- Ran an `oracle_bbox` smoke with `--replay-protocol visibility_challenge`.
- Ran a small clean Grounding-DINO smoke with the same protocol.
- Wrote an experiment report for the protocol smoke.

Reason:
- The protocol needed to be verified inside Habitat before running a larger
  detector/memory comparison.

Verification:
- Remote focused test passed in `conda habitat`: 29 tests.
- Oracle smoke wrote 117 trace rows and 9 replay summaries.
- Grounding-DINO smoke wrote 117 trace rows and 9 replay summaries.
- Parsed `summary.json` and `rgb_noise_trace.csv` for both outputs.

Effect:
- In both smokes, `depart` and `non_confirm` have zero target-visible rows,
  while `revisit` is fully target-visible.
- With `oracle_bbox`, hidden phases produce `unknown` rather than
  `non_confirmation`; the evidence classifier still treats target-out-of-view
  as unknown.
- Grounding-DINO produces hidden-phase positives despite zero oracle target
  visibility, exposing detector false-positive pressure that the shared gate
  rejects.

Follow-up:
- Run a larger `1280x720` Grounding-DINO visibility-challenge comparison.
- Export hidden-phase detector positives before interpreting false-trust
  behavior as a memory result.

## 2026-05-28 - Run 1280x720 Visibility-Challenge Grounding-DINO Replay

File:
- `docs/experiments/2026-05-28-memory-only-grounding-dino-replay-1280x720.zh.html`
- `docs/experiments/2026-05-28-visibility-challenge-replay-smoke.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Ran a larger `1280x720` Grounding-DINO visibility-challenge replay with
  `bed,toilet,plant`, two structured episodes per category, three noise
  levels, and `on,naive_count,off`.
- Ran a bed-only hidden-phase debug export and reviewed a 12-image contact
  sheet locally.
- Updated the visibility-challenge experiment report.

Reason:
- The protocol smoke showed the hidden interval works. The next question was
  whether the protocol changes the memory-vs-`naive_count` comparison under a
  real detector and realistic resolution.

Verification:
- Full replay completed with 702 trace rows and 54 replay summaries.
- Bed debug export completed with 156 trace rows, 12 replay summaries, and 79
  debug PNGs.
- Parsed `summary.json` and `rgb_noise_trace.csv` from both outputs.
- Visually inspected `/tmp/dual_anchor_hidden_bed_debug/contact_sheet.png`.

Effect:
- `depart` and `non_confirm` have zero target-visible rows in the full replay;
  `revisit` has 216/216 target-visible rows.
- Memory `on` beats `naive_count` on gated success rows (`93` vs `86`) in this
  run, but also has more gate rejections (`122` vs `111`).
- Hidden-phase positives are all `bed`: 108 hidden bed positives with zero
  oracle target-visible rows; plant and toilet have zero hidden positives.
- PNG inspection suggests these bed hidden positives are Grounding-DINO false
  positives on door/furniture regions, not just strict Habitat GT on a visible
  bed.

Follow-up:
- Add a trace-filtered debug export for hidden-phase positives so the next
  diagnostic run does not need broad gate-rejection export.
- Decide whether to add an expected-location-empty evidence context that can
  create true `NON_CONFIRMATION` without giving `naive_count` any extra logic.

## 2026-05-28 - Add Trace-Filtered Debug PNG Export

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added trace-filtered debug PNG export controls:
  `debug_export_replay_phases` and `debug_export_evidence_types`.
- Kept `debug_export_categories` shared across gate-rejection and
  trace-filtered exports.
- Preserved the old `debug_export_gate_rejections` mode and directory when it
  is used alone; trace-filtered exports write to `debug_rows/`.
- Added CLI flags `--debug-export-replay-phases` and
  `--debug-export-evidence-types`.
- Added phase/evidence fields to debug PNG filenames and metadata text.

Reason:
- The visibility-challenge run showed hidden-phase `bed` positives, but the
  old PNG export could capture them only indirectly through broad gate
  rejections. The next diagnostic needs direct export of
  `replay_phase in {depart, non_confirm}` and `evidence_type=positive` rows.

Verification:
- Wrote failing tests for preflight summary fields and the new export predicate.
- Confirmed the tests failed before implementation:
  `3 failed, 29 passed`.
- Implemented the filters and reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  `32` tests passed.
- Reran focused local tests including grid trace: `35` tests passed.
- Ran CLI preflight with `--debug-export-replay-phases depart,non_confirm`
  and `--debug-export-evidence-types positive`; summary recorded
  `debug_export_directory=debug_rows`.
- Ran compile and whitespace checks.

Effect:
- The next Linux run can export hidden detector positives directly without
  exporting every gate rejection.
- The feature is diagnostic-only; it does not change replay, detector,
  evidence, memory, `naive_count`, or gate behavior.

Follow-up:
- Pull this commit on `badger-linux` and run a small bed-only
  visibility-challenge diagnostic with `--debug-export-replay-phases
  depart,non_confirm` and `--debug-export-evidence-types positive`.

## 2026-05-28 - Run Trace-Filtered Hidden-Bed Positive Diagnostic

File:
- `docs/experiments/2026-05-28-visibility-challenge-replay-smoke.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Pulled commit `d89ebf6` on `badger-linux`.
- Ran the focused tests in `conda habitat`.
- Ran a bed-only `1280x720` Grounding-DINO visibility-challenge replay with
  trace-filtered debug export:
  `replay_phase=depart,non_confirm` and `evidence_type=positive`.
- Copied the exported PNGs locally and generated a contact sheet at
  `/tmp/dual_anchor_hidden_bed_positive_debug/contact_sheet.png`.
- Updated the Chinese HTML status report with the visibility-challenge and
  trace-filtered export status.

Reason:
- The previous broad gate-rejection export suggested hidden `bed` positives
  were detector false positives. The new filter needed to prove it could export
  exactly hidden positive rows and support a direct visual read.

Verification:
- Remote focused tests passed in `conda habitat`: `32` tests.
- The diagnostic output wrote `26` trace rows and `12` debug PNGs under
  `debug_rows/`.
- Parsed `summary.json` and `rgb_noise_trace.csv`:
  - selected episodes: `3`, `33`
  - `depart`: `4` rows, `0` target-visible, `4` exported positives
  - `non_confirm`: `8` rows, `0` target-visible, `8` exported positives
  - no exported `confirm` or `revisit` rows
- Visually inspected all 12 PNGs in the contact sheet.

Effect:
- The trace-filtered export works as intended.
- The bed hidden positives are Grounding-DINO false positives on non-bed
  structures, not Habitat GT being too strict around a visible bed.

Follow-up:
- Run the same trace-filtered hidden-positive export across the remaining
  selected categories at a controlled cap if we need broader detector/GT
  evidence.
- The algorithm question now moves back to evidence semantics: decide whether
  to add an expected-location-empty context that can produce true
  `NON_CONFIRMATION` without giving that logic to `naive_count`.

## 2026-05-28 - Add Geodesic Path Long-Range Replay Protocol

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added `replay_protocol=geodesic_path`.
- Added CLI/config parameter `--geodesic-path-max-steps`.
- The protocol queries Habitat-Sim's navmesh shortest path from the configured
  start to the goal viewpoint, downsamples waypoints, teleports through
  `approach` frames, and finishes with repeated goal-viewpoint `confirm`
  frames.
- Episode summaries now include approximate replay `path_translation_m` plus
  official episode geodesic/euclidean metadata when available.
- The replay loop initializes motion accounting from the first replay step
  when that step is a teleport, so path summaries do not count temporary
  measurement poses used while constructing the replay.

Reason:
- The previous visibility challenge was a useful memory/evidence stress test,
  but it was still a local teleport protocol. The project needs a long-range
  bridge from official ObjectNav starts before moving to a full action-level
  follower or robot run.

Verification:
- Wrote failing tests for the new preflight parameter, path resampling, and
  geodesic replay step generation.
- Confirmed the new tests failed before implementation:
  `3 failed, 32 passed`.
- Implemented the protocol and reran focused local tests:
  `39` tests passed.
- Ran CLI preflight with `--replay-protocol geodesic_path` and
  `--geodesic-path-max-steps 12`; summary recorded `approach` phase and the
  max-step value.
- Ran compile and whitespace checks.

Effect:
- The next Linux run can replay official episode-start to goal-viewpoint
  paths while keeping detector, RGB/depth noise, memory modes, `naive_count`,
  and the shared gate unchanged.
- This is still not official Habitat ObjectNav success/SPL because waypoints
  are teleported rather than reached by an action-level policy.

Follow-up:
- Pull the commit on `badger-linux`.
- Run an `oracle_bbox` geodesic-path smoke first, then a small Grounding-DINO
  geodesic-path replay from `episode_start`.

## 2026-05-28 - Add Current-Positive Opportunistic Trust

File:
- `src/objectnav_core/objectnav_core/memory/usability.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_semantic_yolo_stress.py`
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_usability_memory.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added `DecisionContext.current_positive_evidence`.
- Added a shared `UsabilityDecisionPolicy` shortcut: if the current frame has
  positive evidence and the mode's own `p_valid` is at least `0.88`, choose
  `TRUST` instead of another `VERIFY`.
- Passed current positive evidence from the Habitat RGB-noise runner into the
  shared decision context.

Reason:
- The first Grounding-DINO `geodesic_path` smoke showed that `memory=on`
  reduced gate rejections compared with `naive_count` but missed some
  target-visible success rows because it waited one extra verification frame
  after strong current positive evidence.

Verification:
- Wrote a failing policy test showing that the same high-validity belief stays
  `VERIFY` without current positive evidence but becomes `TRUST` with current
  positive evidence.
- Confirmed the test failed before implementation.
- Implemented the shared policy rule and reran focused local tests:
  `49` tests passed.
- Ran compile and whitespace checks.

Effect:
- This should reduce over-conservatism for `memory=on` in long-range replay
  without relaxing the current-positive gate or adding non-confirmation logic
  to `naive_count`.
- The rule is shared by all memory modes, so it is not a baseline-only or
  algorithm-only post-hoc metric change.

Follow-up:
- Pull on `badger-linux` and rerun the Grounding-DINO `geodesic_path` smoke to
  check success/gate-rejection tradeoff.

## 2026-05-28 - Add Delayed Birth To RGB-Noise Memory Replay

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added explicit candidate-birth state for `memory=on` in the RGB-noise
  replay harness.
- Fresh default memory now ignores pre-birth non-positive evidence until the
  first `POSITIVE` detector observation creates a candidate.
- Beliefs loaded from SQLite that differ from the default are treated as
  already born, preserving lifelong carry-over across episodes.
- `naive_count` remains unchanged and still only counts positive observations.

Reason:
- The first `geodesic_path` Grounding-DINO smoke exposed a fairness bug in the
  long-range setup: approach frames before the first visible/detected target
  decayed `memory=on`, while `naive_count` was unaffected because it ignores
  every non-positive event. That is not the intended delayed-birth algorithm
  boundary.

Verification:
- Wrote failing tests for pre-birth non-positive evidence and post-birth
  negative evidence behavior; the focused suite failed with the expected
  missing helper.
- Implemented the helper and reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  `38` tests passed.
- Ran focused local tests:
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_usability_memory.py src/objectnav_core/tests/test_lifelong_memory_harness.py -q`;
  `49` tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`.
- Ran geodesic-path preflight for `1280x720`, Grounding-DINO, and
  `on,naive_count,off`; summary wrote
  `runs/habitat_usability/geodesic_delayed_birth_preflight/summary.json`.

Effect:
- Long-range replay no longer penalizes `memory=on` before a candidate object
  has been born.
- Once born, memory still decays under non-confirmation/free/access-blocked or
  scene-change evidence, so stale-memory handling remains an algorithm
  contribution.

Follow-up:
- Pull this commit on `badger-linux` and rerun the same Grounding-DINO
  `geodesic_path` smoke to compare `memory=on` against `naive_count`.

## 2026-05-28 - Add Long-Range Replay Timing Metrics

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/experiments/2026-05-28-geodesic-path-grounding-dino-replay.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added per-replay timing fields to episode summaries:
  first positive, first raw trust, first gated trust, first oracle-stop
  success, and path translation to first success.
- Added run-level `memory_mode_metrics` summarizing episodes, successful
  episodes, success rows, raw trust rows, gate rejections, mean first-success
  step, mean path-to-first-success, and mean final `p_valid`.
- Recorded the first Grounding-DINO `geodesic_path` smoke result, including
  the current-positive and delayed-birth reruns.

Reason:
- The delayed-birth rerun did not change the small smoke because first positive
  evidence appears early. A long-range lifelong claim needs efficiency metrics
  in addition to success-row counts, otherwise cross-episode memory cannot show
  reduced verification delay or shorter path-to-stop.

Verification:
- Wrote failing tests for the new episode timing helper and
  `memory_mode_metrics`; the focused suite initially failed on missing fields.
- Implemented the reporting helpers and reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  `39` tests passed.
- Ran broader focused local verification:
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py src/objectnav_core/tests/test_usability_memory.py src/objectnav_core/tests/test_lifelong_memory_harness.py -q`;
  `50` tests passed.
- Ran `python3 -m compileall -q src/objectnav_core/objectnav_core`.
- Ran geodesic timing preflight and `git diff --check`.
- Pulled commit `5d0513f` on `badger-linux`, reran the same focused tests in
  `conda habitat`; `50` tests passed.
- Reran the Grounding-DINO `geodesic_path` smoke with timing metrics:
  output
  `runs/habitat_usability/geodesic_path_grounding_dino_smoke_1280x720_cap384_timing`.
  `memory=on` and `naive_count` both succeeded in `3/3` episodes, but
  `memory=on` had fewer success rows (`11` vs `13`) and later mean first
  success (`8.666667` vs `7.666667` steps).

Effect:
- Future long-range replay summaries can compare memory modes by both
  reliability and efficiency without changing detector, evidence, memory,
  `naive_count`, or gate behavior.
- The timing rerun confirms this small smoke is not a memory-win result:
  memory is more conservative, especially for `plant`.

Follow-up:
- Implement an expected-location-empty or stale-memory challenge before
  scaling to a larger paper-facing matrix.

## 2026-05-28 - Add Expected-Empty Replay Challenge

File:
- `src/objectnav_core/objectnav_core/evaluation/habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/objectnav_core/cli/run_habitat_objectnav_rgb_noise_stress.py`
- `src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py`
- `docs/design/2026-05-27-rgb-noise-sim-to-real-objectnav-memory-validation.md`
- `docs/devlog/2026-05.md`
- `docs/handoff/2026-05-27-rgb-noise-sim-to-real-validation.md`

Change:
- Added `replay_protocol=expected_empty_challenge`.
- The protocol emits `confirm`, `expected_empty`, and `revisit` phases from
  measured target-visible and target-hidden viewpoints.
- Added `ReplayStep.expected_target_absent` and a trace row field of the same
  name.
- During expected-empty frames, target-absent frames without detector positive
  evidence are converted to `NON_CONFIRMATION` with reason
  `expected_location_empty`.
- Detector positives in expected-empty frames remain positive, so false
  positives are still visible in the trace.

Reason:
- The geodesic smoke showed that repeated-positive long-range replay does not
  expose stale-memory or expected-empty behavior. The memory algorithm's
  contribution includes using negative evidence after a remembered-location
  verification; the harness needed an explicit way to create that evidence in
  Habitat without giving `naive_count` any negative-evidence logic.

Verification:
- Wrote failing tests for preflight protocol registration, replay step
  expected-empty marking, conversion to `NON_CONFIRMATION`, and preserving
  detector positives.
- Confirmed the focused suite failed before implementation:
  `4 failed, 39 passed`.
- Implemented the protocol and context helper, then reran
  `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=src/objectnav_core python3 -m pytest src/objectnav_core/tests/test_habitat_objectnav_rgb_noise_stress.py -q`;
  `43` tests passed.

Effect:
- The next Linux smoke can test whether expected-empty frames produce true
  `NON_CONFIRMATION` and whether `memory=on` reduces stale trust relative to
  positive-only `naive_count`.

Follow-up:
- Run an `oracle_bbox` expected-empty smoke first, then a Grounding-DINO
  expected-empty smoke at `1280x720`.
